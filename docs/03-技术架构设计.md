# 技术架构设计

## 1. 技术栈选型

### 1.1 核心技术栈

| 技术 | 选型 | 说明 |
|------|------|------|
| 运行时 | .NET 10 | 最新 LTS 版本，性能优秀 |
| 编程语言 | C# | 强类型，生态丰富 |
| LLM 接口 | OpenAI API 兼容 | 灵活切换不同供应商 |
| AI 封装 | Microsoft.Extensions.AI | 轻量级，不引入重框架 |
| 向量存储 | AWS Knowledge Base | 托管服务，运维成本低 |
| 数据库 | PostgreSQL | 可靠稳定，支持 JSON |
| ORM | EF Core | .NET 生态标准 ORM |

### 1.2 技术选型原则

**为什么不用 Microsoft Agent Framework？**
- 框架过重，引入不必要的复杂性
- 学习曲线陡峭
- 定制化困难
- Microsoft.Extensions.AI 足够满足需求

**为什么选择 AWS Knowledge Base？**
- 托管服务，无需自建向量数据库
- 与 AWS 生态集成良好
- 支持多种数据源接入
- 内置 RAG 能力

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Application Layer (SRE Agent)                     │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                    Business Agents (SRE 具体实现)                │    │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐    │    │
│  │  │ Diagnostic│  │  Log      │  │  Metrics  │  │ Playbook  │    │    │
│  │  │  Agent    │  │  Agent    │  │  Agent    │  │  Agent    │    │    │
│  │  └───────────┘  └───────────┘  └───────────┘  └───────────┘    │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                  │                                       │
│  ┌───────────────────────────────┼───────────────────────────────────┐  │
│  │                    Business Tools (SRE 工具实现)                   │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐     │  │
│  │  │ Slack   │ │Prometheus│ │Sumo Logic│ │   K8s   │ │   AWS   │     │  │
│  │  │ Tool    │ │ Tool    │ │ Tool    │ │  Tool   │ │  Tool   │     │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │ 依赖
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         Agent Framework (通用框架)                       │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      Core Abstractions                            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │   │
│  │  │  IAgent     │  │  ITool      │  │ IContext    │               │   │
│  │  │             │  │             │  │  Manager    │               │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      Base Implementations                         │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │   │
│  │  │ToolLoopAgent│  │ AgentAsTool │  │  Default    │               │   │
│  │  │ (基础Agent) │  │  Wrapper    │  │ContextMgr  │               │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                         Hooks System                              │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │   │
│  │  │ BeforeCall  │  │ AfterCall   │  │  OnError    │  ...          │   │
│  │  │   Hook      │  │   Hook      │  │   Hook      │               │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                      Core Services                                │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │   │
│  │  │   Event     │  │   Result    │  │  Session    │               │   │
│  │  │    Bus      │  │   Types     │  │   Store     │               │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         Infrastructure Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                      │
│  │ PostgreSQL  │  │    AWS      │  │   OpenAI    │                      │
│  │ (Persist)   │  │ Knowledge   │  │ Compatible  │                      │
│  │             │  │    Base     │  │    LLM      │                      │
│  └─────────────┘  └─────────────┘  └─────────────┘                      │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 架构分层

| 层次 | 职责 | 说明 |
|------|------|------|
| Application Layer | 具体业务实现 | SRE 相关的 Agent 和 Tool 实现 |
| Agent Framework | 通用 Agent 框架 | 提供抽象、基础实现、Hooks 等 |
| Infrastructure | 基础设施 | 数据库、LLM、向量存储等 |

### 2.3 关键设计原则

1. **框架与业务分离**：Agent Framework 不包含任何具体业务逻辑
2. **可扩展性**：通过接口和 Hooks 允许业务层自定义行为
3. **Result 模式**：所有操作返回 Result，不抛异常，保证 LLM 能获得完整错误信息
4. **轻量级通信**：使用 SSE 而非 SignalR 等重框架

---

## 3. Agent Framework 设计

### 3.1 核心抽象

#### 3.1.1 Agent 接口

```csharp
/// <summary>
/// Agent 的核心抽象接口
/// </summary>
public interface IAgent
{
    /// <summary>Agent 唯一标识</summary>
    string Id { get; }
    
    /// <summary>Agent 名称</summary>
    string Name { get; }
    
    /// <summary>Agent 描述（用于 System Prompt 或作为 Tool 时的描述）</summary>
    string Description { get; }
    
    /// <summary>执行 Agent</summary>
    Task<AgentResult> ExecuteAsync(
        AgentExecutionContext context,
        CancellationToken cancellationToken = default);
}
```

#### 3.1.2 Agent 配置选项

```csharp
/// <summary>
/// Agent 配置选项，用于自定义 Agent 行为
/// </summary>
public class AgentOptions
{
    /// <summary>使用的模型标识</summary>
    public string Model { get; set; } = "gpt-4";
    
    /// <summary>System Prompt</summary>
    public string? SystemPrompt { get; set; }
    
    /// <summary>最大迭代次数（Tool Loop 的最大轮数）</summary>
    public int MaxIterations { get; set; } = 10;
    
    /// <summary>温度参数</summary>
    public double Temperature { get; set; } = 0.7;
    
    /// <summary>单次请求最大 Token</summary>
    public int? MaxTokens { get; set; }
    
    /// <summary>执行超时时间</summary>
    public TimeSpan Timeout { get; set; } = TimeSpan.FromMinutes(5);
    
    /// <summary>可用的工具列表</summary>
    public IReadOnlyList<ITool> Tools { get; set; } = Array.Empty<ITool>();
    
    /// <summary>上下文管理器（可选，不提供则使用默认）</summary>
    public IContextManager? ContextManager { get; set; }
    
    /// <summary>自定义元数据</summary>
    public Dictionary<string, object> Metadata { get; set; } = new();
}
```

#### 3.1.3 Agent 执行上下文

```csharp
/// <summary>
/// Agent 执行时的上下文信息
/// </summary>
public class AgentExecutionContext
{
    /// <summary>会话 ID</summary>
    public Guid SessionId { get; init; }
    
    /// <summary>用户输入/任务描述</summary>
    public string Input { get; init; } = string.Empty;
    
    /// <summary>初始消息历史（可选）</summary>
    public IReadOnlyList<Message>? InitialMessages { get; init; }
    
    /// <summary>执行时传递的变量</summary>
    public Dictionary<string, object> Variables { get; init; } = new();
    
    /// <summary>父 Agent ID（用于多 Agent 场景）</summary>
    public string? ParentAgentId { get; init; }
}
```

#### 3.1.4 Agent 执行结果

```csharp
/// <summary>
/// Agent 执行结果（使用 Result 模式）
/// </summary>
public class AgentResult
{
    public bool IsSuccess { get; init; }
    public string? Output { get; init; }
    public AgentError? Error { get; init; }
    
    /// <summary>执行过程中产生的所有消息</summary>
    public IReadOnlyList<Message> Messages { get; init; } = Array.Empty<Message>();
    
    /// <summary>使用的总 Token 数</summary>
    public TokenUsage TokenUsage { get; init; } = new();
    
    /// <summary>执行元数据</summary>
    public Dictionary<string, object> Metadata { get; init; } = new();
    
    public static AgentResult Success(string output, IReadOnlyList<Message> messages) 
        => new() { IsSuccess = true, Output = output, Messages = messages };
    
    public static AgentResult Failure(AgentError error, IReadOnlyList<Message> messages) 
        => new() { IsSuccess = false, Error = error, Messages = messages };
}

public record AgentError(string Code, string Message, Exception? Exception = null);

public record TokenUsage(int PromptTokens = 0, int CompletionTokens = 0)
{
    public int TotalTokens => PromptTokens + CompletionTokens;
}
```

---

### 3.2 Tool 抽象

#### 3.2.1 Tool 接口

```csharp
/// <summary>
/// 工具接口（使用 Result 模式，不抛异常）
/// </summary>
public interface ITool
{
    /// <summary>工具名称（用于 LLM 调用）</summary>
    string Name { get; }
    
    /// <summary>工具描述</summary>
    string Description { get; }
    
    /// <summary>参数 JSON Schema</summary>
    JsonElement ParameterSchema { get; }
    
    /// <summary>
    /// 执行工具（返回 Result，不抛异常）
    /// </summary>
    Task<ToolResult> ExecuteAsync(
        ToolExecutionContext context,
        CancellationToken cancellationToken = default);
}
```

#### 3.2.2 Tool 执行结果（Result 模式）

```csharp
/// <summary>
/// 工具执行结果（使用 Result 模式）
/// 即使执行失败，也返回结果而非抛异常，确保 LLM 能获得错误信息
/// </summary>
public class ToolResult
{
    /// <summary>是否成功</summary>
    public bool IsSuccess { get; init; }
    
    /// <summary>结果内容（成功时的返回值，或失败时的错误描述）</summary>
    public string Content { get; init; } = string.Empty;
    
    /// <summary>结构化数据（可选）</summary>
    public object? Data { get; init; }
    
    /// <summary>错误代码（失败时）</summary>
    public string? ErrorCode { get; init; }
    
    /// <summary>是否可重试</summary>
    public bool IsRetryable { get; init; }
    
    /// <summary>执行耗时</summary>
    public TimeSpan Duration { get; init; }
    
    /// <summary>元数据</summary>
    public Dictionary<string, object> Metadata { get; init; } = new();
    
    /// <summary>创建成功结果</summary>
    public static ToolResult Success(string content, object? data = null)
        => new() { IsSuccess = true, Content = content, Data = data };
    
    /// <summary>创建失败结果（供 LLM 理解并可能重试）</summary>
    public static ToolResult Failure(
        string errorMessage, 
        string? errorCode = null, 
        bool isRetryable = true)
        => new() 
        { 
            IsSuccess = false, 
            Content = errorMessage,
            ErrorCode = errorCode,
            IsRetryable = isRetryable
        };
    
    /// <summary>从异常创建失败结果</summary>
    public static ToolResult FromException(Exception ex, bool isRetryable = true)
        => Failure(
            $"Tool execution failed: {ex.Message}", 
            ex.GetType().Name,
            isRetryable);
}
```

#### 3.2.3 Tool 执行上下文

```csharp
public class ToolExecutionContext
{
    /// <summary>会话 ID</summary>
    public Guid SessionId { get; init; }
    
    /// <summary>调用方 Agent ID</summary>
    public string AgentId { get; init; } = string.Empty;
    
    /// <summary>工具参数（已解析的 JSON）</summary>
    public JsonElement Parameters { get; init; }
    
    /// <summary>原始参数字符串</summary>
    public string RawArguments { get; init; } = string.Empty;
    
    /// <summary>执行变量（从 Agent 传递）</summary>
    public IReadOnlyDictionary<string, object> Variables { get; init; } 
        = new Dictionary<string, object>();
}
```

---

### 3.3 上下文管理抽象

#### 3.3.1 上下文管理器接口

```csharp
/// <summary>
/// 上下文管理器接口，Framework 提供默认实现，业务层可扩展
/// </summary>
public interface IContextManager
{
    /// <summary>添加消息</summary>
    Task AddMessageAsync(Message message, CancellationToken cancellationToken = default);
    
    /// <summary>获取用于发送给 LLM 的消息列表（可能经过裁剪）</summary>
    Task<IReadOnlyList<Message>> GetMessagesForCompletionAsync(
        int maxTokens,
        CancellationToken cancellationToken = default);
    
    /// <summary>获取完整消息历史</summary>
    Task<IReadOnlyList<Message>> GetAllMessagesAsync(
        CancellationToken cancellationToken = default);
    
    /// <summary>获取当前 Token 使用量估算</summary>
    int EstimatedTokenCount { get; }
    
    /// <summary>清空上下文</summary>
    Task ClearAsync(CancellationToken cancellationToken = default);
}
```

#### 3.3.2 默认实现

```csharp
/// <summary>
/// 默认上下文管理器实现
/// </summary>
public class DefaultContextManager : IContextManager
{
    private readonly List<Message> _messages = new();
    private readonly ITokenEstimator _tokenEstimator;
    private readonly ContextManagerOptions _options;
    
    public DefaultContextManager(
        ITokenEstimator tokenEstimator,
        ContextManagerOptions? options = null)
    {
        _tokenEstimator = tokenEstimator;
        _options = options ?? new ContextManagerOptions();
    }
    
    public int EstimatedTokenCount => _tokenEstimator.EstimateTokens(_messages);
    
    public Task AddMessageAsync(Message message, CancellationToken cancellationToken = default)
    {
        message.Metadata.EstimatedTokens = _tokenEstimator.EstimateTokens(message);
        _messages.Add(message);
        return Task.CompletedTask;
    }
    
    public Task<IReadOnlyList<Message>> GetMessagesForCompletionAsync(
        int maxTokens,
        CancellationToken cancellationToken = default)
    {
        // 如果未超限，直接返回
        if (EstimatedTokenCount <= maxTokens)
        {
            return Task.FromResult<IReadOnlyList<Message>>(_messages.ToList());
        }
        
        // 需要裁剪
        return Task.FromResult(TrimMessages(maxTokens));
    }
    
    private IReadOnlyList<Message> TrimMessages(int maxTokens)
    {
        var result = new List<Message>();
        var currentTokens = 0;
        
        // 始终保留 System 消息
        var systemMessages = _messages.Where(m => m.Role == MessageRole.System).ToList();
        foreach (var msg in systemMessages)
        {
            result.Add(msg);
            currentTokens += msg.Metadata.EstimatedTokens;
        }
        
        // 从后向前添加其他消息
        var otherMessages = _messages
            .Where(m => m.Role != MessageRole.System)
            .Reverse()
            .ToList();
        
        foreach (var msg in otherMessages)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens > maxTokens)
            {
                break;
            }
            result.Insert(systemMessages.Count, msg); // 插入到 system 消息之后
            currentTokens += msg.Metadata.EstimatedTokens;
        }
        
        return result;
    }
    
    public Task<IReadOnlyList<Message>> GetAllMessagesAsync(
        CancellationToken cancellationToken = default)
    {
        return Task.FromResult<IReadOnlyList<Message>>(_messages.ToList());
    }
    
    public Task ClearAsync(CancellationToken cancellationToken = default)
    {
        _messages.Clear();
        return Task.CompletedTask;
    }
}

public class ContextManagerOptions
{
    /// <summary>默认最大 Token 数</summary>
    public int DefaultMaxTokens { get; set; } = 100000;
    
    /// <summary>裁剪策略</summary>
    public TrimStrategy TrimStrategy { get; set; } = TrimStrategy.RemoveOldest;
}

public enum TrimStrategy
{
    RemoveOldest,
    RemoveByPriority,
    Summarize
}
```

---

### 3.4 Hooks 系统

#### 3.4.1 Hooks 接口定义

```csharp
/// <summary>
/// Agent 生命周期 Hooks，允许业务层在各阶段介入
/// </summary>
public interface IAgentHooks
{
    /// <summary>Agent 执行开始前</summary>
    Task<HookResult> OnBeforeExecuteAsync(BeforeExecuteContext context);
    
    /// <summary>LLM 调用前（可修改请求参数）</summary>
    Task<HookResult> OnBeforeLLMCallAsync(BeforeLLMCallContext context);
    
    /// <summary>LLM 调用后</summary>
    Task OnAfterLLMCallAsync(AfterLLMCallContext context);
    
    /// <summary>Tool 调用前</summary>
    Task<HookResult> OnBeforeToolCallAsync(BeforeToolCallContext context);
    
    /// <summary>Tool 调用后</summary>
    Task OnAfterToolCallAsync(AfterToolCallContext context);
    
    /// <summary>每次迭代结束时</summary>
    Task<HookResult> OnIterationEndAsync(IterationEndContext context);
    
    /// <summary>Agent 执行完成后</summary>
    Task OnAfterExecuteAsync(AfterExecuteContext context);
    
    /// <summary>发生错误时</summary>
    Task OnErrorAsync(ErrorContext context);
}

/// <summary>
/// Hook 返回结果，可控制是否继续执行
/// </summary>
public class HookResult
{
    public bool ShouldContinue { get; init; } = true;
    public string? AbortReason { get; init; }
    
    /// <summary>修改后的上下文（可选）</summary>
    public object? ModifiedContext { get; init; }
    
    public static HookResult Continue() => new() { ShouldContinue = true };
    public static HookResult Abort(string reason) => new() { ShouldContinue = false, AbortReason = reason };
    public static HookResult ContinueWith(object modifiedContext) 
        => new() { ShouldContinue = true, ModifiedContext = modifiedContext };
}
```

#### 3.4.2 Hook 上下文定义

```csharp
/// <summary>
/// LLM 调用前的 Hook 上下文，可修改调用参数
/// </summary>
public class BeforeLLMCallContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public int IterationNumber { get; init; }
    
    /// <summary>当前使用的模型（可修改）</summary>
    public string Model { get; set; } = string.Empty;
    
    /// <summary>温度参数（可修改）</summary>
    public double Temperature { get; set; }
    
    /// <summary>最大 Token（可修改）</summary>
    public int? MaxTokens { get; set; }
    
    /// <summary>要发送的消息（可修改）</summary>
    public List<Message> Messages { get; set; } = new();
    
    /// <summary>可用的工具（可修改）</summary>
    public List<ITool> Tools { get; set; } = new();
    
    /// <summary>自定义数据</summary>
    public Dictionary<string, object> CustomData { get; set; } = new();
}

/// <summary>
/// LLM 调用后的 Hook 上下文
/// </summary>
public class AfterLLMCallContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public int IterationNumber { get; init; }
    public string Model { get; init; } = string.Empty;
    public Message Response { get; init; } = null!;
    public TokenUsage TokenUsage { get; init; } = new();
    public TimeSpan Duration { get; init; }
}

/// <summary>
/// Tool 调用前的 Hook 上下文
/// </summary>
public class BeforeToolCallContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public string ToolName { get; init; } = string.Empty;
    public JsonElement Parameters { get; init; }
    
    /// <summary>是否需要审批（可修改）</summary>
    public bool RequiresApproval { get; set; }
    
    /// <summary>自定义数据</summary>
    public Dictionary<string, object> CustomData { get; set; } = new();
}

/// <summary>
/// Tool 调用后的 Hook 上下文
/// </summary>
public class AfterToolCallContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public string ToolName { get; init; } = string.Empty;
    public ToolResult Result { get; init; } = null!;
    public TimeSpan Duration { get; init; }
}

/// <summary>
/// 迭代结束时的 Hook 上下文
/// </summary>
public class IterationEndContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public int IterationNumber { get; init; }
    public int MaxIterations { get; init; }
    public TokenUsage TotalTokenUsage { get; init; } = new();
    
    /// <summary>是否应该继续迭代（可修改）</summary>
    public bool ShouldContinue { get; set; } = true;
}

public class BeforeExecuteContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public string Input { get; init; } = string.Empty;
    public AgentOptions Options { get; set; } = null!;
}

public class AfterExecuteContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public AgentResult Result { get; init; } = null!;
    public TimeSpan TotalDuration { get; init; }
}

public class ErrorContext
{
    public Guid SessionId { get; init; }
    public string AgentId { get; init; } = string.Empty;
    public Exception Exception { get; init; } = null!;
    public string Phase { get; init; } = string.Empty; // "llm_call", "tool_call", etc.
}
```

#### 3.4.3 默认 Hooks 实现

```csharp
/// <summary>
/// 默认的空 Hooks 实现，业务层可继承并覆盖需要的方法
/// </summary>
public class DefaultAgentHooks : IAgentHooks
{
    public virtual Task<HookResult> OnBeforeExecuteAsync(BeforeExecuteContext context)
        => Task.FromResult(HookResult.Continue());
    
    public virtual Task<HookResult> OnBeforeLLMCallAsync(BeforeLLMCallContext context)
        => Task.FromResult(HookResult.Continue());
    
    public virtual Task OnAfterLLMCallAsync(AfterLLMCallContext context)
        => Task.CompletedTask;
    
    public virtual Task<HookResult> OnBeforeToolCallAsync(BeforeToolCallContext context)
        => Task.FromResult(HookResult.Continue());
    
    public virtual Task OnAfterToolCallAsync(AfterToolCallContext context)
        => Task.CompletedTask;
    
    public virtual Task<HookResult> OnIterationEndAsync(IterationEndContext context)
        => Task.FromResult(HookResult.Continue());
    
    public virtual Task OnAfterExecuteAsync(AfterExecuteContext context)
        => Task.CompletedTask;
    
    public virtual Task OnErrorAsync(ErrorContext context)
        => Task.CompletedTask;
}
```

#### 3.4.4 Hooks 使用示例

```csharp
/// <summary>
/// 业务层自定义 Hooks 示例：动态切换模型
/// </summary>
public class SreAgentHooks : DefaultAgentHooks
{
    private readonly IModelSelector _modelSelector;
    
    public override async Task<HookResult> OnBeforeLLMCallAsync(BeforeLLMCallContext context)
    {
        // 根据上下文动态选择模型
        var selectedModel = await _modelSelector.SelectModelAsync(
            context.Messages, 
            context.Tools);
        
        context.Model = selectedModel;
        
        // 如果是简单任务，降低温度提高确定性
        if (context.Messages.Count < 5 && context.Tools.Count == 0)
        {
            context.Temperature = 0.3;
        }
        
        return HookResult.Continue();
    }
    
    public override async Task<HookResult> OnBeforeToolCallAsync(BeforeToolCallContext context)
    {
        // 根据工具类型决定是否需要审批
        if (context.ToolName.Contains("write") || context.ToolName.Contains("execute"))
        {
            context.RequiresApproval = true;
        }
        
        return HookResult.Continue();
    }
    
    public override async Task<HookResult> OnIterationEndAsync(IterationEndContext context)
    {
        // Token 使用过多时提前终止
        if (context.TotalTokenUsage.TotalTokens > 50000)
        {
            return HookResult.Abort("Token usage exceeded limit");
        }
        
        return HookResult.Continue();
    }
}
```

---

### 3.5 基础 Agent 实现

#### 3.5.1 ToolLoopAgent（核心基础实现）

```csharp
/// <summary>
/// 工具循环 Agent - Framework 提供的基础实现
/// 实现标准的 ReAct 模式：Think -> Act -> Observe 循环
/// </summary>
public class ToolLoopAgent : IAgent
{
    private readonly IChatClient _chatClient;
    private readonly AgentOptions _options;
    private readonly IAgentHooks _hooks;
    private readonly IContextManager _contextManager;
    private readonly IEventBus _eventBus;
    
    public string Id { get; }
    public string Name { get; }
    public string Description { get; }
    
    public ToolLoopAgent(
        string id,
        string name,
        string description,
        IChatClient chatClient,
        AgentOptions options,
        IAgentHooks? hooks = null,
        IEventBus? eventBus = null)
    {
        Id = id;
        Name = name;
        Description = description;
        _chatClient = chatClient;
        _options = options;
        _hooks = hooks ?? new DefaultAgentHooks();
        _contextManager = options.ContextManager ?? new DefaultContextManager(new TiktokenEstimator());
        _eventBus = eventBus ?? new NullEventBus();
    }
    
    public async Task<AgentResult> ExecuteAsync(
        AgentExecutionContext context,
        CancellationToken cancellationToken = default)
    {
        var allMessages = new List<Message>();
        var totalTokenUsage = new TokenUsage();
        
        try
        {
            // Before Execute Hook
            var beforeCtx = new BeforeExecuteContext
            {
                SessionId = context.SessionId,
                AgentId = Id,
                Input = context.Input,
                Options = _options
            };
            var hookResult = await _hooks.OnBeforeExecuteAsync(beforeCtx);
            if (!hookResult.ShouldContinue)
            {
                return AgentResult.Failure(
                    new AgentError("ABORTED", hookResult.AbortReason ?? "Aborted by hook"),
                    allMessages);
            }
            
            // 初始化上下文
            await InitializeContextAsync(context, cancellationToken);
            
            // 主循环
            for (int iteration = 0; iteration < _options.MaxIterations; iteration++)
            {
                cancellationToken.ThrowIfCancellationRequested();
                
                // 获取用于 LLM 的消息
                var messagesForLLM = await _contextManager.GetMessagesForCompletionAsync(
                    _options.MaxTokens ?? 100000, cancellationToken);
                
                // Before LLM Call Hook
                var llmContext = new BeforeLLMCallContext
                {
                    SessionId = context.SessionId,
                    AgentId = Id,
                    IterationNumber = iteration,
                    Model = _options.Model,
                    Temperature = _options.Temperature,
                    MaxTokens = _options.MaxTokens,
                    Messages = messagesForLLM.ToList(),
                    Tools = _options.Tools.ToList()
                };
                
                hookResult = await _hooks.OnBeforeLLMCallAsync(llmContext);
                if (!hookResult.ShouldContinue)
                {
                    return AgentResult.Failure(
                        new AgentError("ABORTED", hookResult.AbortReason ?? "Aborted by hook"),
                        allMessages);
                }
                
                // 调用 LLM
                var llmResponse = await CallLLMAsync(llmContext, cancellationToken);
                totalTokenUsage = totalTokenUsage with
                {
                    PromptTokens = totalTokenUsage.PromptTokens + llmResponse.TokenUsage.PromptTokens,
                    CompletionTokens = totalTokenUsage.CompletionTokens + llmResponse.TokenUsage.CompletionTokens
                };
                
                // 添加响应消息
                await _contextManager.AddMessageAsync(llmResponse.Message, cancellationToken);
                allMessages.Add(llmResponse.Message);
                
                // After LLM Call Hook
                await _hooks.OnAfterLLMCallAsync(new AfterLLMCallContext
                {
                    SessionId = context.SessionId,
                    AgentId = Id,
                    IterationNumber = iteration,
                    Model = llmContext.Model,
                    Response = llmResponse.Message,
                    TokenUsage = llmResponse.TokenUsage,
                    Duration = llmResponse.Duration
                });
                
                // 检查是否有工具调用
                if (llmResponse.Message.Parts.OfType<ToolCallPart>().Any())
                {
                    // 执行工具调用
                    var toolResults = await ExecuteToolCallsAsync(
                        context.SessionId, 
                        llmResponse.Message.Parts.OfType<ToolCallPart>().ToList(),
                        context.Variables,
                        cancellationToken);
                    
                    // 添加工具结果消息
                    var toolResultMessage = CreateToolResultMessage(toolResults);
                    await _contextManager.AddMessageAsync(toolResultMessage, cancellationToken);
                    allMessages.Add(toolResultMessage);
                }
                else
                {
                    // 没有工具调用，检查是否完成
                    var textContent = llmResponse.Message.Parts
                        .OfType<TextPart>()
                        .Select(p => p.Text)
                        .FirstOrDefault();
                    
                    if (!string.IsNullOrEmpty(textContent))
                    {
                        // Agent 完成
                        await _hooks.OnAfterExecuteAsync(new AfterExecuteContext
                        {
                            SessionId = context.SessionId,
                            AgentId = Id,
                            Result = AgentResult.Success(textContent, allMessages),
                            TotalDuration = TimeSpan.Zero // TODO: track duration
                        });
                        
                        return AgentResult.Success(textContent, allMessages) with
                        {
                            TokenUsage = totalTokenUsage
                        };
                    }
                }
                
                // Iteration End Hook
                var iterEndCtx = new IterationEndContext
                {
                    SessionId = context.SessionId,
                    AgentId = Id,
                    IterationNumber = iteration,
                    MaxIterations = _options.MaxIterations,
                    TotalTokenUsage = totalTokenUsage,
                    ShouldContinue = true
                };
                
                hookResult = await _hooks.OnIterationEndAsync(iterEndCtx);
                if (!hookResult.ShouldContinue || !iterEndCtx.ShouldContinue)
                {
                    break;
                }
            }
            
            // 达到最大迭代次数
            return AgentResult.Failure(
                new AgentError("MAX_ITERATIONS", "Reached maximum iterations without completion"),
                allMessages) with { TokenUsage = totalTokenUsage };
        }
        catch (Exception ex)
        {
            await _hooks.OnErrorAsync(new ErrorContext
            {
                SessionId = context.SessionId,
                AgentId = Id,
                Exception = ex,
                Phase = "execution"
            });
            
            return AgentResult.Failure(
                new AgentError("EXCEPTION", ex.Message, ex),
                allMessages) with { TokenUsage = totalTokenUsage };
        }
    }
    
    private async Task<List<(string ToolCallId, ToolResult Result)>> ExecuteToolCallsAsync(
        Guid sessionId,
        List<ToolCallPart> toolCalls,
        IReadOnlyDictionary<string, object> variables,
        CancellationToken cancellationToken)
    {
        var results = new List<(string, ToolResult)>();
        
        foreach (var toolCall in toolCalls)
        {
            var tool = _options.Tools.FirstOrDefault(t => t.Name == toolCall.Name);
            
            ToolResult result;
            if (tool == null)
            {
                // 工具不存在，返回错误结果（不抛异常）
                result = ToolResult.Failure(
                    $"Tool '{toolCall.Name}' not found. Available tools: {string.Join(", ", _options.Tools.Select(t => t.Name))}",
                    "TOOL_NOT_FOUND");
            }
            else
            {
                // Before Tool Call Hook
                var beforeCtx = new BeforeToolCallContext
                {
                    SessionId = sessionId,
                    AgentId = Id,
                    ToolName = toolCall.Name,
                    Parameters = JsonDocument.Parse(toolCall.Arguments).RootElement
                };
                
                var hookResult = await _hooks.OnBeforeToolCallAsync(beforeCtx);
                if (!hookResult.ShouldContinue)
                {
                    result = ToolResult.Failure(
                        hookResult.AbortReason ?? "Tool call aborted by hook",
                        "ABORTED_BY_HOOK",
                        isRetryable: false);
                }
                else
                {
                    // 执行工具（捕获所有异常，转为 Result）
                    var sw = System.Diagnostics.Stopwatch.StartNew();
                    try
                    {
                        var toolContext = new ToolExecutionContext
                        {
                            SessionId = sessionId,
                            AgentId = Id,
                            Parameters = beforeCtx.Parameters,
                            RawArguments = toolCall.Arguments,
                            Variables = variables
                        };
                        
                        result = await tool.ExecuteAsync(toolContext, cancellationToken);
                    }
                    catch (Exception ex)
                    {
                        // 即使工具抛异常，也转为 Result 返回给 LLM
                        result = ToolResult.FromException(ex);
                    }
                    sw.Stop();
                    result = result with { Duration = sw.Elapsed };
                    
                    // After Tool Call Hook
                    await _hooks.OnAfterToolCallAsync(new AfterToolCallContext
                    {
                        SessionId = sessionId,
                        AgentId = Id,
                        ToolName = toolCall.Name,
                        Result = result,
                        Duration = sw.Elapsed
                    });
                }
            }
            
            results.Add((toolCall.Id, result));
        }
        
        return results;
    }
    
    // ... 其他私有方法
}
```

#### 3.5.2 AgentAsTool（将 Agent 包装为 Tool）

```csharp
/// <summary>
/// 将 Agent 包装为 Tool，用于多 Agent 协作
/// </summary>
public class AgentAsTool : ITool
{
    private readonly IAgent _agent;
    private readonly AgentAsToolOptions _options;
    
    public string Name { get; }
    public string Description { get; }
    public JsonElement ParameterSchema { get; }
    
    public AgentAsTool(IAgent agent, AgentAsToolOptions? options = null)
    {
        _agent = agent;
        _options = options ?? new AgentAsToolOptions();
        
        Name = options?.ToolName ?? $"delegate_to_{agent.Id}";
        Description = options?.ToolDescription ?? agent.Description;
        ParameterSchema = BuildParameterSchema();
    }
    
    public async Task<ToolResult> ExecuteAsync(
        ToolExecutionContext context,
        CancellationToken cancellationToken = default)
    {
        try
        {
            var input = context.Parameters.GetProperty("task").GetString() ?? "";
            
            var agentContext = new AgentExecutionContext
            {
                SessionId = context.SessionId,
                Input = input,
                Variables = new Dictionary<string, object>(context.Variables),
                ParentAgentId = context.AgentId
            };
            
            var result = await _agent.ExecuteAsync(agentContext, cancellationToken);
            
            if (result.IsSuccess)
            {
                return ToolResult.Success(
                    result.Output ?? "Agent completed without output",
                    new { AgentId = _agent.Id, Messages = result.Messages });
            }
            else
            {
                return ToolResult.Failure(
                    $"Agent failed: {result.Error?.Message}",
                    result.Error?.Code,
                    isRetryable: true);
            }
        }
        catch (Exception ex)
        {
            return ToolResult.FromException(ex);
        }
    }
    
    private JsonElement BuildParameterSchema()
    {
        var schema = new
        {
            type = "object",
            properties = new
            {
                task = new
                {
                    type = "string",
                    description = $"The task to delegate to {_agent.Name}"
                }
            },
            required = new[] { "task" }
        };
        
        return JsonSerializer.SerializeToElement(schema);
    }
}

public class AgentAsToolOptions
{
    public string? ToolName { get; set; }
    public string? ToolDescription { get; set; }
}
```

---

## 4. 数据模型

### 4.1 核心实体：Session - Message - Part 三层结构

```
┌─────────────────────────────────────────────────────────────────┐
│                          Session                                 │
│  - Id: Guid                                                      │
│  - Status: SessionStatus                                         │
│  - Metadata: JsonDocument                                        │
│  - CreatedAt, UpdatedAt                                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │ 1:N
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                          Message                                 │
│  - Id: Guid                                                      │
│  - SessionId: Guid                                               │
│  - Role: MessageRole (system/user/assistant/tool)                │
│  - Metadata: MessageMetadata                                     │
│  - CreatedAt                                                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │ 1:N
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                           Part                                   │
│  - Id: Guid                                                      │
│  - MessageId: Guid                                               │
│  - Type: PartType (text/tool_call/tool_result/image/...)        │
│  - Content: string / JsonDocument                                │
│  - Metadata: JsonDocument                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 实体定义

```csharp
/// <summary>
/// 会话实体
/// </summary>
public class Session
{
    public Guid Id { get; set; }
    public SessionStatus Status { get; set; }
    public string? AgentId { get; set; }
    public JsonDocument? Metadata { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }
    
    public ICollection<Message> Messages { get; set; } = new List<Message>();
}

public enum SessionStatus
{
    Created,
    Running,
    WaitingInput,
    Completed,
    Failed,
    Cancelled
}

/// <summary>
/// 消息实体
/// </summary>
public class Message
{
    public Guid Id { get; set; }
    public Guid SessionId { get; set; }
    public MessageRole Role { get; set; }
    public MessageMetadata Metadata { get; set; } = new();
    public DateTime CreatedAt { get; set; }
    
    public Session Session { get; set; } = null!;
    public ICollection<Part> Parts { get; set; } = new List<Part>();
}

public enum MessageRole
{
    System,
    User,
    Assistant,
    Tool
}

public class MessageMetadata
{
    public string? AgentId { get; set; }
    public int EstimatedTokens { get; set; }
    public int Priority { get; set; }
    public HashSet<string> Tags { get; set; } = new();
    public Dictionary<string, object> Extra { get; set; } = new();
}

/// <summary>
/// 消息部分实体 - 支持多模态内容
/// </summary>
public class Part
{
    public Guid Id { get; set; }
    public Guid MessageId { get; set; }
    public PartType Type { get; set; }
    public int Index { get; set; }  // 在消息中的顺序
    
    // 根据 Type 使用不同字段
    public string? TextContent { get; set; }
    public JsonDocument? JsonContent { get; set; }
    public byte[]? BinaryContent { get; set; }
    public string? MimeType { get; set; }
    
    public JsonDocument? Metadata { get; set; }
    
    public Message Message { get; set; } = null!;
}

public enum PartType
{
    Text,
    ToolCall,
    ToolResult,
    Image,
    File,
    Error
}
```

### 4.3 Part 类型的运行时表示

```csharp
/// <summary>
/// Part 的运行时抽象基类
/// </summary>
public abstract class PartBase
{
    public abstract PartType Type { get; }
}

public class TextPart : PartBase
{
    public override PartType Type => PartType.Text;
    public string Text { get; init; } = string.Empty;
}

public class ToolCallPart : PartBase
{
    public override PartType Type => PartType.ToolCall;
    public string Id { get; init; } = string.Empty;
    public string Name { get; init; } = string.Empty;
    public string Arguments { get; init; } = "{}";
}

public class ToolResultPart : PartBase
{
    public override PartType Type => PartType.ToolResult;
    public string ToolCallId { get; init; } = string.Empty;
    public string ToolName { get; init; } = string.Empty;
    public ToolResult Result { get; init; } = null!;
}

public class ImagePart : PartBase
{
    public override PartType Type => PartType.Image;
    public string MimeType { get; init; } = "image/png";
    public byte[]? Data { get; init; }
    public string? Url { get; init; }
}

public class ErrorPart : PartBase
{
    public override PartType Type => PartType.Error;
    public string ErrorCode { get; init; } = string.Empty;
    public string ErrorMessage { get; init; } = string.Empty;
}
```

### 4.4 数据库表设计

```sql
-- 会话表
CREATE TABLE sessions (
    id UUID PRIMARY KEY,
    status VARCHAR(50) NOT NULL,
    agent_id VARCHAR(100),
    metadata JSONB,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_sessions_status ON sessions(status);
CREATE INDEX idx_sessions_created_at ON sessions(created_at);

-- 消息表
CREATE TABLE messages (
    id UUID PRIMARY KEY,
    session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
    role VARCHAR(50) NOT NULL,
    metadata JSONB NOT NULL DEFAULT '{}',
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_messages_session ON messages(session_id, created_at);

-- 消息部分表
CREATE TABLE parts (
    id UUID PRIMARY KEY,
    message_id UUID NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
    type VARCHAR(50) NOT NULL,
    index INTEGER NOT NULL DEFAULT 0,
    text_content TEXT,
    json_content JSONB,
    binary_content BYTEA,
    mime_type VARCHAR(100),
    metadata JSONB,
    
    CONSTRAINT unique_message_part_index UNIQUE (message_id, index)
);

CREATE INDEX idx_parts_message ON parts(message_id);
CREATE INDEX idx_parts_type ON parts(type);
```

---

## 5. 事件系统

### 5.1 事件接口（轻量级设计）

```csharp
/// <summary>
/// 事件总线接口（轻量级）
/// </summary>
public interface IEventBus
{
    /// <summary>发布事件</summary>
    Task PublishAsync<T>(T @event, CancellationToken cancellationToken = default) 
        where T : IAgentEvent;
    
    /// <summary>订阅事件</summary>
    IDisposable Subscribe<T>(Func<T, CancellationToken, Task> handler) 
        where T : IAgentEvent;
}

/// <summary>
/// 事件基接口
/// </summary>
public interface IAgentEvent
{
    Guid EventId { get; }
    Guid SessionId { get; }
    DateTime OccurredAt { get; }
}
```

### 5.2 SSE 输出支持

```csharp
/// <summary>
/// SSE 事件流写入器
/// </summary>
public interface ISseWriter
{
    Task WriteEventAsync<T>(T @event, CancellationToken cancellationToken = default)
        where T : IAgentEvent;
    
    Task WriteDataAsync(string data, string? eventType = null, 
        CancellationToken cancellationToken = default);
    
    Task FlushAsync(CancellationToken cancellationToken = default);
}

/// <summary>
/// SSE 事件流实现
/// </summary>
public class SseWriter : ISseWriter
{
    private readonly Stream _stream;
    private readonly StreamWriter _writer;
    
    public SseWriter(Stream stream)
    {
        _stream = stream;
        _writer = new StreamWriter(stream, Encoding.UTF8, leaveOpen: true);
    }
    
    public async Task WriteEventAsync<T>(T @event, CancellationToken cancellationToken)
        where T : IAgentEvent
    {
        var eventType = typeof(T).Name.ToLowerInvariant();
        var data = JsonSerializer.Serialize(@event);
        await WriteDataAsync(data, eventType, cancellationToken);
    }
    
    public async Task WriteDataAsync(string data, string? eventType, 
        CancellationToken cancellationToken)
    {
        if (!string.IsNullOrEmpty(eventType))
        {
            await _writer.WriteLineAsync($"event: {eventType}");
        }
        
        // SSE 数据行
        foreach (var line in data.Split('\n'))
        {
            await _writer.WriteLineAsync($"data: {line}");
        }
        
        await _writer.WriteLineAsync(); // 空行表示事件结束
        await _writer.FlushAsync(cancellationToken);
    }
    
    public async Task FlushAsync(CancellationToken cancellationToken)
    {
        await _writer.FlushAsync(cancellationToken);
    }
}
```

### 5.3 API 端点（SSE 支持）

```csharp
[ApiController]
[Route("api/sessions")]
public class SessionController : ControllerBase
{
    /// <summary>
    /// 执行 Agent 并通过 SSE 流式返回事件
    /// </summary>
    [HttpPost("{sessionId}/execute")]
    public async Task ExecuteWithStream(
        Guid sessionId,
        [FromBody] ExecuteRequest request,
        CancellationToken cancellationToken)
    {
        Response.ContentType = "text/event-stream";
        Response.Headers.CacheControl = "no-cache";
        Response.Headers.Connection = "keep-alive";
        
        var sseWriter = new SseWriter(Response.Body);
        
        // 订阅事件并转发到 SSE
        using var subscription = _eventBus.Subscribe<IAgentEvent>(async (@event, ct) =>
        {
            if (@event.SessionId == sessionId)
            {
                await sseWriter.WriteEventAsync(@event, ct);
            }
        });
        
        // 执行 Agent
        var result = await _agentService.ExecuteAsync(sessionId, request.Input, cancellationToken);
        
        // 发送完成事件
        await sseWriter.WriteDataAsync(
            JsonSerializer.Serialize(result), 
            "complete", 
            cancellationToken);
    }
}
```

---

## 6. 配置示例

```yaml
# Agent Framework 配置
agent_framework:
  default_model: "gpt-4"
  default_max_iterations: 10
  default_temperature: 0.7
  default_timeout_seconds: 300
  
  context_manager:
    default_max_tokens: 100000
    trim_strategy: "remove_oldest"
    
  events:
    bus_type: "in_memory"  # 或 "redis"

# 业务层配置 (SRE Agent)
sre_agent:
  agents:
    diagnostic:
      model: "gpt-4"
      system_prompt_file: "prompts/diagnostic.md"
      max_iterations: 15
      
    log_analysis:
      model: "gpt-4o-mini"
      system_prompt_file: "prompts/log_analysis.md"
      max_iterations: 5
      
  tools:
    sumo_logic:
      enabled: true
      timeout_seconds: 30
      
    prometheus:
      enabled: true
      timeout_seconds: 15
      
  hooks:
    dynamic_model_selection: true
    approval_required_tools:
      - "k8s_scale"
      - "aws_restart"
```

---

## 7. 总结

### 7.1 架构要点

1. **Framework 与业务分离**
   - Agent Framework 只提供抽象和基础实现
   - 不包含任何 SRE 具体业务逻辑
   - 业务 Agent 在 Application Layer 实现

2. **Result 模式**
   - 所有 Tool 执行返回 `ToolResult`，不抛异常
   - 确保 LLM 能获得完整的错误信息进行重试

3. **Hooks 系统**
   - 允许业务层在 Agent 执行各阶段介入
   - 支持动态修改模型、参数等
   - 默认实现为空操作，易于扩展

4. **三层数据模型**
   - Session → Message → Part
   - 支持多模态内容
   - 灵活的元数据扩展

5. **轻量级通信**
   - 使用 SSE 替代 SignalR
   - Framework 层不引入重量级框架
   - 业务层可根据需要选择通信方式
