# 上下文管理策略

## 1. 概述

上下文管理是 AI Agent 系统的核心挑战之一。Agent Framework 提供：
- **抽象接口**：`IContextManager` 定义标准契约
- **默认实现**：`DefaultContextManager` 满足通用需求
- **可扩展性**：业务层可自定义实现
- **Token 限制管理**：基于模型最大 Token 数的自动剪枝
- **可插拔剪枝策略**：`IContextTrimmer` 接口支持自定义剪枝逻辑

## 2. 数据模型：Session - Message - Part

### 2.1 三层结构

```
Session (会话)
    │
    ├── Message (消息)
    │       │
    │       ├── Part (文本部分)
    │       ├── Part (工具调用)
    │       └── Part (工具结果)
    │
    ├── Message
    │       │
    │       └── Part (文本部分)
    │
    └── ...
```

### 2.2 Message 定义

```csharp
/// <summary>
/// 消息 - 对话中的一条消息
/// </summary>
public class Message
{
    public Guid Id { get; set; } = Guid.NewGuid();
    public MessageRole Role { get; set; }
    public List<PartBase> Parts { get; set; } = new();
    public MessageMetadata Metadata { get; set; } = new();
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}

public enum MessageRole
{
    System,
    User,
    Assistant,
    Tool
}

public class MessageMetadata
{
    /// <summary>来源 Agent ID</summary>
    public string? AgentId { get; set; }
    
    /// <summary>预估 Token 数</summary>
    public int EstimatedTokens { get; set; }
    
    /// <summary>优先级（裁剪时参考）</summary>
    public int Priority { get; set; } = MessagePriority.Normal;
    
    /// <summary>是否可删除</summary>
    public bool IsDeletable { get; set; } = true;
    
    /// <summary>标签（用于分类和过滤）</summary>
    public HashSet<string> Tags { get; set; } = new();
    
    /// <summary>自定义扩展数据</summary>
    public Dictionary<string, object> Extra { get; set; } = new();
}

public static class MessagePriority
{
    public const int Critical = 100;    // 绝不删除
    public const int Important = 80;    // 高优先级保留
    public const int Normal = 50;       // 正常优先级
    public const int Low = 20;          // 低优先级
    public const int Disposable = 0;    // 可丢弃
}
```

### 2.3 Part 定义

```csharp
/// <summary>
/// Part 基类 - 消息的组成部分
/// </summary>
public abstract class PartBase
{
    public Guid Id { get; set; } = Guid.NewGuid();
    public abstract PartType Type { get; }
}

public enum PartType
{
    Text,
    ToolCall,
    ToolResult,
    Image,
    File,
    Error
}

/// <summary>
/// 文本部分
/// </summary>
public class TextPart : PartBase
{
    public override PartType Type => PartType.Text;
    public string Text { get; init; } = string.Empty;
}

/// <summary>
/// 工具调用部分
/// </summary>
public class ToolCallPart : PartBase
{
    public override PartType Type => PartType.ToolCall;
    public string ToolCallId { get; init; } = string.Empty;
    public string Name { get; init; } = string.Empty;
    public string Arguments { get; init; } = "{}";
}

/// <summary>
/// 工具结果部分
/// </summary>
public class ToolResultPart : PartBase
{
    public override PartType Type => PartType.ToolResult;
    public string ToolCallId { get; init; } = string.Empty;
    public string ToolName { get; init; } = string.Empty;
    public bool IsSuccess { get; init; }
    public string Content { get; init; } = string.Empty;
    
    /// <summary>从 ToolResult 创建</summary>
    public static ToolResultPart FromToolResult(string toolCallId, string toolName, ToolResult result)
        => new()
        {
            ToolCallId = toolCallId,
            ToolName = toolName,
            IsSuccess = result.IsSuccess,
            Content = result.Content
        };
}

/// <summary>
/// 图片部分
/// </summary>
public class ImagePart : PartBase
{
    public override PartType Type => PartType.Image;
    public string MimeType { get; init; } = "image/png";
    public byte[]? Data { get; init; }
    public string? Url { get; init; }
}

/// <summary>
/// 错误部分
/// </summary>
public class ErrorPart : PartBase
{
    public override PartType Type => PartType.Error;
    public string ErrorCode { get; init; } = string.Empty;
    public string ErrorMessage { get; init; } = string.Empty;
}
```

---

## 3. 模型 Token 限制配置

### 3.1 ModelProviderOptions 扩展

在 `ModelProviderOptions` 中增加模型最大 Token 数的配置：

```csharp
/// <summary>
/// Model Provider 配置选项
/// 包含 API 连接信息、各能力级别对应的模型映射和定价信息
/// </summary>
public class ModelProviderOptions
{
    /// <summary>Provider 名称（用于标识）</summary>
    public required string Name { get; init; }
    
    /// <summary>API Base URL</summary>
    public required string BaseUrl { get; init; }
    
    /// <summary>API Key（直接配置）</summary>
    public string? ApiKey { get; init; }
    
    /// <summary>API Key 对应的环境变量名</summary>
    public string? ApiKeyEnvironmentVariable { get; init; }
    
    /// <summary>
    /// 各能力级别对应的模型映射
    /// Key: ModelCapability, Value: 模型名称
    /// </summary>
    public required Dictionary<ModelCapability, string> Models { get; init; }
    
    /// <summary>
    /// 各模型的 Token 限制配置
    /// Key: 模型名称, Value: Token 限制信息
    /// </summary>
    public Dictionary<string, ModelTokenLimits> TokenLimits { get; init; } = new();
    
    /// <summary>
    /// 各模型的定价信息
    /// Key: 模型名称, Value: 定价信息
    /// </summary>
    public Dictionary<string, ModelPricing> Pricing { get; init; } = new();
    
    /// <summary>获取指定模型的 Token 限制</summary>
    public ModelTokenLimits GetTokenLimits(string modelName)
    {
        if (TokenLimits.TryGetValue(modelName, out var limits))
        {
            return limits;
        }
        
        // 返回默认值
        return ModelTokenLimits.Default;
    }
    
    /// <summary>获取指定能力级别对应模型的 Token 限制</summary>
    public ModelTokenLimits GetTokenLimits(ModelCapability capability)
    {
        var modelName = GetModel(capability);
        return GetTokenLimits(modelName);
    }
    
    // ... 其他现有方法
}

/// <summary>
/// 模型 Token 限制配置
/// </summary>
public class ModelTokenLimits
{
    /// <summary>模型最大上下文窗口（输入 + 输出）</summary>
    public int MaxContextTokens { get; init; } = 128000;
    
    /// <summary>模型最大输出 Token 数</summary>
    public int MaxOutputTokens { get; init; } = 8192;
    
    /// <summary>
    /// 预留 Buffer 比例（0-1）
    /// 用于预留一定空间给输出和估算误差
    /// </summary>
    public double ReservedBufferRatio { get; init; } = 0.1;
    
    /// <summary>
    /// 计算可用于输入的最大 Token 数
    /// = MaxContextTokens - MaxOutputTokens - Buffer
    /// </summary>
    public int EffectiveInputTokens
    {
        get
        {
            var buffer = (int)(MaxContextTokens * ReservedBufferRatio);
            return MaxContextTokens - MaxOutputTokens - buffer;
        }
    }
    
    /// <summary>默认配置（适用于大多数现代模型）</summary>
    public static ModelTokenLimits Default => new()
    {
        MaxContextTokens = 128000,
        MaxOutputTokens = 8192,
        ReservedBufferRatio = 0.1
    };
    
    /// <summary>Claude 3.5 Sonnet 配置</summary>
    public static ModelTokenLimits Claude35Sonnet => new()
    {
        MaxContextTokens = 200000,
        MaxOutputTokens = 8192,
        ReservedBufferRatio = 0.1
    };
    
    /// <summary>GPT-4o 配置</summary>
    public static ModelTokenLimits Gpt4o => new()
    {
        MaxContextTokens = 128000,
        MaxOutputTokens = 16384,
        ReservedBufferRatio = 0.1
    };
}
```

### 3.2 配置示例

```yaml
model_providers:
  - name: "anthropic"
    base_url: "https://api.anthropic.com"
    api_key_env: "ANTHROPIC_API_KEY"
    models:
      large: "claude-sonnet-4-20250514"
      medium: "claude-sonnet-4-20250514"
      small: "claude-haiku-3-20240307"
    token_limits:
      claude-sonnet-4-20250514:
        max_context_tokens: 200000
        max_output_tokens: 8192
        reserved_buffer_ratio: 0.1
      claude-haiku-3-20240307:
        max_context_tokens: 200000
        max_output_tokens: 4096
        reserved_buffer_ratio: 0.1
        
  - name: "openai"
    base_url: "https://api.openai.com/v1"
    api_key_env: "OPENAI_API_KEY"
    models:
      large: "gpt-4o"
      medium: "gpt-4o-mini"
      small: "gpt-4o-mini"
    token_limits:
      gpt-4o:
        max_context_tokens: 128000
        max_output_tokens: 16384
        reserved_buffer_ratio: 0.1
      gpt-4o-mini:
        max_context_tokens: 128000
        max_output_tokens: 16384
        reserved_buffer_ratio: 0.1
```

---

## 4. 上下文管理器接口

### 4.1 IContextManager 接口定义

```csharp
/// <summary>
/// 上下文管理器接口
/// Framework 提供默认实现，业务层可扩展
/// </summary>
public interface IContextManager
{
    /// <summary>添加消息到上下文</summary>
    Task AddMessageAsync(Message message, CancellationToken cancellationToken = default);
    
    /// <summary>批量添加消息</summary>
    Task AddMessagesAsync(IEnumerable<Message> messages, CancellationToken cancellationToken = default);
    
    /// <summary>
    /// 获取用于发送给 LLM 的消息列表
    /// 可能经过裁剪以满足 Token 限制
    /// </summary>
    Task<IReadOnlyList<Message>> GetMessagesForCompletionAsync(
        int maxTokens,
        CancellationToken cancellationToken = default);
    
    /// <summary>获取完整消息历史（未裁剪）</summary>
    Task<IReadOnlyList<Message>> GetAllMessagesAsync(
        CancellationToken cancellationToken = default);
    
    /// <summary>当前 Token 使用量估算</summary>
    int EstimatedTokenCount { get; }
    
    /// <summary>清空上下文</summary>
    Task ClearAsync(CancellationToken cancellationToken = default);
    
    /// <summary>设置 System 消息（会替换已有的）</summary>
    Task SetSystemMessageAsync(string content, CancellationToken cancellationToken = default);
}
```

### 4.2 配置选项

```csharp
/// <summary>
/// 上下文管理器配置选项
/// </summary>
public class ContextManagerOptions
{
    /// <summary>默认最大 Token 数</summary>
    public int DefaultMaxTokens { get; set; } = 100000;
    
    /// <summary>触发裁剪的阈值（占 MaxTokens 的比例）</summary>
    public double TrimThreshold { get; set; } = 0.8;
    
    /// <summary>裁剪后的目标（占 MaxTokens 的比例）</summary>
    public double TrimTarget { get; set; } = 0.6;
    
    /// <summary>裁剪策略</summary>
    public TrimStrategy TrimStrategy { get; set; } = TrimStrategy.RemoveOldest;
    
    /// <summary>是否自动压缩长工具结果</summary>
    public bool AutoCompressToolResults { get; set; } = true;
    
    /// <summary>工具结果压缩阈值（Token 数）</summary>
    public int ToolResultCompressThreshold { get; set; } = 1000;
}

public enum TrimStrategy
{
    /// <summary>移除最早的消息</summary>
    RemoveOldest,
    
    /// <summary>按优先级移除</summary>
    RemoveByPriority,
    
    /// <summary>总结历史消息</summary>
    Summarize,
    
    /// <summary>滑动窗口</summary>
    SlidingWindow
}
```

---

## 5. Agent 循环中的 Token 检查机制

### 5.1 设计原则

在 Agent 执行 Tool Loop 时，每次发送请求给 LLM 之前，都需要：

1. **估算当前上下文 Token 数**：包括 System Prompt、历史消息、工具结果等
2. **检查是否超出模型限制**：对比模型的 `EffectiveInputTokens`
3. **预留 Buffer 空间**：为输出和估算误差预留空间
4. **触发剪枝**：如果超出限制，调用 `IContextTrimmer` 进行剪枝

### 5.2 Token 检查流程

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent Tool Loop                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1. 准备发送请求给 LLM                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 2. 估算当前上下文 Token 数                           │   │
│  │    - System Prompt                                   │   │
│  │    - 历史消息                                        │   │
│  │    - 工具定义                                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 3. 获取模型 Token 限制                               │   │
│  │    effectiveLimit = MaxContext - MaxOutput - Buffer  │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 4. 检查是否需要剪枝                                  │   │
│  │    if (estimatedTokens > effectiveLimit)             │   │
│  │        → 调用 IContextTrimmer.TrimAsync()            │   │
│  └─────────────────────────────────────────────────────┘   │
│                          │                                  │
│                          ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 5. 发送请求给 LLM                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 ToolLoopAgent 中的实现

```csharp
/// <summary>
/// 支持 Tool Loop 的 Agent 基类
/// </summary>
public abstract class ToolLoopAgent : IAgent
{
    private readonly IContextManager _contextManager;
    private readonly IContextTrimmer _contextTrimmer;
    private readonly ITokenEstimator _tokenEstimator;
    private readonly ModelProviderOptions _modelProviderOptions;
    
    /// <summary>
    /// 执行单次 LLM 调用前的 Token 检查和剪枝
    /// </summary>
    protected async Task<IReadOnlyList<Message>> PrepareMessagesForCompletionAsync(
        ModelCapability capability,
        CancellationToken cancellationToken = default)
    {
        // 1. 获取模型 Token 限制
        var tokenLimits = _modelProviderOptions.GetTokenLimits(capability);
        var effectiveLimit = tokenLimits.EffectiveInputTokens;
        
        // 2. 估算当前上下文 Token 数
        var currentTokens = _contextManager.EstimatedTokenCount;
        
        // 3. 估算工具定义的 Token 数（如果有）
        var toolDefinitionTokens = EstimateToolDefinitionTokens();
        var totalEstimatedTokens = currentTokens + toolDefinitionTokens;
        
        // 4. 检查是否需要剪枝
        if (totalEstimatedTokens > effectiveLimit)
        {
            Logger.LogInformation(
                "Token limit exceeded. Current: {Current}, Limit: {Limit}. Triggering context trimming.",
                totalEstimatedTokens, effectiveLimit);
            
            // 计算剪枝目标（保留一定余量）
            var targetTokens = (int)(effectiveLimit * 0.8) - toolDefinitionTokens;
            
            // 调用剪枝器
            await _contextTrimmer.TrimAsync(
                _contextManager,
                targetTokens,
                cancellationToken);
        }
        
        // 5. 获取剪枝后的消息列表
        return await _contextManager.GetMessagesForCompletionAsync(
            effectiveLimit - toolDefinitionTokens,
            cancellationToken);
    }
    
    /// <summary>
    /// Tool Loop 主循环
    /// </summary>
    protected async Task<AgentResult> ExecuteToolLoopAsync(
        string userInput,
        CancellationToken cancellationToken = default)
    {
        // 添加用户输入
        await _contextManager.AddMessageAsync(new Message
        {
            Role = MessageRole.User,
            Parts = new List<PartBase> { new TextPart { Text = userInput } }
        }, cancellationToken);
        
        while (!cancellationToken.IsCancellationRequested)
        {
            // 准备消息（包含 Token 检查和剪枝）
            var messages = await PrepareMessagesForCompletionAsync(
                ModelCapability.Large,
                cancellationToken);
            
            // 调用 LLM
            var response = await CallLlmAsync(messages, cancellationToken);
            
            // 添加助手响应到上下文
            await _contextManager.AddMessageAsync(response.Message, cancellationToken);
            
            // 检查是否有工具调用
            var toolCalls = ExtractToolCalls(response.Message);
            if (!toolCalls.Any())
            {
                // 没有工具调用，返回最终结果
                return AgentResult.Success(response.Message);
            }
            
            // 执行工具调用
            var toolResults = await ExecuteToolCallsAsync(toolCalls, cancellationToken);
            
            // 添加工具结果到上下文
            await _contextManager.AddMessageAsync(new Message
            {
                Role = MessageRole.Tool,
                Parts = toolResults.Select(r => (PartBase)r).ToList()
            }, cancellationToken);
        }
        
        return AgentResult.Cancelled();
    }
    
    // ... 其他方法
}
```

---

## 6. 可扩展的上下文剪枝策略

### 6.1 IContextTrimmer 接口

Framework 提供 `IContextTrimmer` 接口，每种剪枝策略都是一个独立的实现类：

```csharp
/// <summary>
/// 上下文剪枝器接口
/// 每种剪枝策略实现一个独立的类
/// </summary>
public interface IContextTrimmer
{
    /// <summary>策略名称</summary>
    string Name { get; }
    
    /// <summary>
    /// 对上下文进行剪枝
    /// </summary>
    /// <param name="contextManager">上下文管理器</param>
    /// <param name="targetTokens">剪枝后的目标 Token 数</param>
    /// <param name="cancellationToken">取消令牌</param>
    /// <returns>剪枝结果</returns>
    Task<TrimResult> TrimAsync(
        IContextManager contextManager,
        int targetTokens,
        CancellationToken cancellationToken = default);
}

/// <summary>
/// 剪枝结果
/// </summary>
public class TrimResult
{
    /// <summary>是否成功</summary>
    public bool IsSuccess { get; init; }
    
    /// <summary>剪枝前的 Token 数</summary>
    public int TokensBefore { get; init; }
    
    /// <summary>剪枝后的 Token 数</summary>
    public int TokensAfter { get; init; }
    
    /// <summary>被移除的消息数量</summary>
    public int MessagesRemoved { get; init; }
    
    /// <summary>剪枝策略名称</summary>
    public string StrategyUsed { get; init; } = string.Empty;
    
    /// <summary>错误信息（如果失败）</summary>
    public string? ErrorMessage { get; init; }
    
    public static TrimResult Success(int tokensBefore, int tokensAfter, int messagesRemoved, string strategy)
        => new()
        {
            IsSuccess = true,
            TokensBefore = tokensBefore,
            TokensAfter = tokensAfter,
            MessagesRemoved = messagesRemoved,
            StrategyUsed = strategy
        };
    
    public static TrimResult Failure(string errorMessage)
        => new()
        {
            IsSuccess = false,
            ErrorMessage = errorMessage
        };
}
```

### 6.2 Framework 提供的剪枝器实现

Framework 提供多个独立的剪枝器实现，每个实现一种策略：

#### 6.2.1 RemoveOldestContextTrimmer（默认）

移除最早的消息，保留最新的消息：

```csharp
/// <summary>
/// 移除最早消息的剪枝器（默认策略）
/// 从后向前保留消息，优先保留最新的对话内容
/// </summary>
public class RemoveOldestContextTrimmer : IContextTrimmer
{
    private readonly ILogger<RemoveOldestContextTrimmer> _logger;
    
    public string Name => "RemoveOldest";
    
    public RemoveOldestContextTrimmer(ILogger<RemoveOldestContextTrimmer>? logger = null)
    {
        _logger = logger ?? NullLogger<RemoveOldestContextTrimmer>.Instance;
    }
    
    public async Task<TrimResult> TrimAsync(
        IContextManager contextManager,
        int targetTokens,
        CancellationToken cancellationToken = default)
    {
        var allMessages = await contextManager.GetAllMessagesAsync(cancellationToken);
        var tokensBefore = allMessages.Sum(m => m.Metadata.EstimatedTokens);
        
        if (tokensBefore <= targetTokens)
        {
            return TrimResult.Success(tokensBefore, tokensBefore, 0, Name);
        }
        
        _logger.LogInformation(
            "Starting {Strategy} trimming. Current: {Current}, Target: {Target}",
            Name, tokensBefore, targetTokens);
        
        var messagesToKeep = new List<Message>();
        var currentTokens = 0;
        var messagesRemoved = 0;
        
        // 始终保留 System 消息
        var systemMsg = allMessages.FirstOrDefault(m => m.Role == MessageRole.System);
        if (systemMsg != null)
        {
            messagesToKeep.Add(systemMsg);
            currentTokens += systemMsg.Metadata.EstimatedTokens;
        }
        
        // 从后向前遍历，保留最新的消息
        var nonSystemMessages = allMessages
            .Where(m => m.Role != MessageRole.System)
            .Reverse()
            .ToList();
        
        foreach (var msg in nonSystemMessages)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens <= targetTokens)
            {
                messagesToKeep.Insert(systemMsg != null ? 1 : 0, msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
            else if (!msg.Metadata.IsDeletable)
            {
                // 不可删除的消息强制保留
                messagesToKeep.Insert(systemMsg != null ? 1 : 0, msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
            else
            {
                messagesRemoved++;
            }
        }
        
        // 重建上下文
        await contextManager.ClearAsync(cancellationToken);
        await contextManager.AddMessagesAsync(messagesToKeep, cancellationToken);
        
        _logger.LogInformation(
            "{Strategy} trimming completed. Before: {Before}, After: {After}, Removed: {Removed}",
            Name, tokensBefore, currentTokens, messagesRemoved);
        
        return TrimResult.Success(tokensBefore, currentTokens, messagesRemoved, Name);
    }
}
```

#### 6.2.2 PriorityBasedContextTrimmer

按消息优先级剪枝，优先保留高优先级消息：

```csharp
/// <summary>
/// 基于优先级的剪枝器
/// 优先保留高优先级消息，同优先级按时间倒序
/// </summary>
public class PriorityBasedContextTrimmer : IContextTrimmer
{
    private readonly ILogger<PriorityBasedContextTrimmer> _logger;
    
    public string Name => "PriorityBased";
    
    public PriorityBasedContextTrimmer(ILogger<PriorityBasedContextTrimmer>? logger = null)
    {
        _logger = logger ?? NullLogger<PriorityBasedContextTrimmer>.Instance;
    }
    
    public async Task<TrimResult> TrimAsync(
        IContextManager contextManager,
        int targetTokens,
        CancellationToken cancellationToken = default)
    {
        var allMessages = await contextManager.GetAllMessagesAsync(cancellationToken);
        var tokensBefore = allMessages.Sum(m => m.Metadata.EstimatedTokens);
        
        if (tokensBefore <= targetTokens)
        {
            return TrimResult.Success(tokensBefore, tokensBefore, 0, Name);
        }
        
        _logger.LogInformation(
            "Starting {Strategy} trimming. Current: {Current}, Target: {Target}",
            Name, tokensBefore, targetTokens);
        
        // 按优先级排序（高优先级在前），同优先级按时间倒序
        var sorted = allMessages
            .OrderByDescending(m => m.Metadata.Priority)
            .ThenByDescending(m => m.CreatedAt)
            .ToList();
        
        var messagesToKeep = new List<Message>();
        var currentTokens = 0;
        
        foreach (var msg in sorted)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens <= targetTokens || !msg.Metadata.IsDeletable)
            {
                messagesToKeep.Add(msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
        }
        
        // 按时间重新排序，保持对话顺序
        messagesToKeep = messagesToKeep.OrderBy(m => m.CreatedAt).ToList();
        
        var messagesRemoved = allMessages.Count - messagesToKeep.Count;
        
        // 重建上下文
        await contextManager.ClearAsync(cancellationToken);
        await contextManager.AddMessagesAsync(messagesToKeep, cancellationToken);
        
        _logger.LogInformation(
            "{Strategy} trimming completed. Before: {Before}, After: {After}, Removed: {Removed}",
            Name, tokensBefore, currentTokens, messagesRemoved);
        
        return TrimResult.Success(tokensBefore, currentTokens, messagesRemoved, Name);
    }
}
```

#### 6.2.3 SlidingWindowContextTrimmer

滑动窗口策略，保留最近的连续消息：

```csharp
/// <summary>
/// 滑动窗口剪枝器
/// 保留最近的连续消息，遇到超限立即停止
/// </summary>
public class SlidingWindowContextTrimmer : IContextTrimmer
{
    private readonly ILogger<SlidingWindowContextTrimmer> _logger;
    
    public string Name => "SlidingWindow";
    
    public SlidingWindowContextTrimmer(ILogger<SlidingWindowContextTrimmer>? logger = null)
    {
        _logger = logger ?? NullLogger<SlidingWindowContextTrimmer>.Instance;
    }
    
    public async Task<TrimResult> TrimAsync(
        IContextManager contextManager,
        int targetTokens,
        CancellationToken cancellationToken = default)
    {
        var allMessages = await contextManager.GetAllMessagesAsync(cancellationToken);
        var tokensBefore = allMessages.Sum(m => m.Metadata.EstimatedTokens);
        
        if (tokensBefore <= targetTokens)
        {
            return TrimResult.Success(tokensBefore, tokensBefore, 0, Name);
        }
        
        _logger.LogInformation(
            "Starting {Strategy} trimming. Current: {Current}, Target: {Target}",
            Name, tokensBefore, targetTokens);
        
        var messagesToKeep = new List<Message>();
        var currentTokens = 0;
        
        // 保留 System 消息
        var systemMsg = allMessages.FirstOrDefault(m => m.Role == MessageRole.System);
        if (systemMsg != null)
        {
            messagesToKeep.Add(systemMsg);
            currentTokens += systemMsg.Metadata.EstimatedTokens;
        }
        
        // 从后向前，连续保留消息直到超限
        var nonSystemMessages = allMessages
            .Where(m => m.Role != MessageRole.System)
            .Reverse()
            .ToList();
        
        foreach (var msg in nonSystemMessages)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens <= targetTokens)
            {
                messagesToKeep.Insert(systemMsg != null ? 1 : 0, msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
            else
            {
                // 滑动窗口策略：遇到第一个超限消息就停止
                break;
            }
        }
        
        var messagesRemoved = allMessages.Count - messagesToKeep.Count;
        
        // 重建上下文
        await contextManager.ClearAsync(cancellationToken);
        await contextManager.AddMessagesAsync(messagesToKeep, cancellationToken);
        
        _logger.LogInformation(
            "{Strategy} trimming completed. Before: {Before}, After: {After}, Removed: {Removed}",
            Name, tokensBefore, currentTokens, messagesRemoved);
        
        return TrimResult.Success(tokensBefore, currentTokens, messagesRemoved, Name);
    }
}
```

### 6.3 ToolLoopAgent 中的剪枝器配置

`ToolLoopAgent` 通过 Options 接收 `IContextTrimmer` 实例，并提供默认实现：

```csharp
/// <summary>
/// ToolLoopAgent 配置选项
/// </summary>
public class ToolLoopAgentOptions
{
    /// <summary>
    /// 上下文剪枝器
    /// 如果不指定，使用 RemoveOldestContextTrimmer 作为默认实现
    /// </summary>
    public IContextTrimmer? ContextTrimmer { get; set; }
    
    /// <summary>最大循环次数</summary>
    public int MaxIterations { get; set; } = 50;
    
    /// <summary>剪枝后的目标 Token 比例（相对于模型限制）</summary>
    public double TrimTargetRatio { get; set; } = 0.8;
}

/// <summary>
/// 支持 Tool Loop 的 Agent 基类
/// </summary>
public abstract class ToolLoopAgent : IAgent
{
    private readonly IContextManager _contextManager;
    private readonly IContextTrimmer _contextTrimmer;
    private readonly ITokenEstimator _tokenEstimator;
    private readonly ModelProviderOptions _modelProviderOptions;
    private readonly ToolLoopAgentOptions _options;
    private readonly ILogger _logger;
    
    protected ToolLoopAgent(
        IContextManager contextManager,
        ITokenEstimator tokenEstimator,
        ModelProviderOptions modelProviderOptions,
        ToolLoopAgentOptions? options = null,
        ILogger? logger = null)
    {
        _contextManager = contextManager;
        _tokenEstimator = tokenEstimator;
        _modelProviderOptions = modelProviderOptions;
        _options = options ?? new ToolLoopAgentOptions();
        _logger = logger ?? NullLogger.Instance;
        
        // 使用传入的剪枝器，或创建默认的 RemoveOldestContextTrimmer
        _contextTrimmer = _options.ContextTrimmer 
            ?? new RemoveOldestContextTrimmer();
    }
    
    /// <summary>
    /// 执行单次 LLM 调用前的 Token 检查和剪枝
    /// </summary>
    protected async Task<IReadOnlyList<Message>> PrepareMessagesForCompletionAsync(
        ModelCapability capability,
        CancellationToken cancellationToken = default)
    {
        // 1. 获取模型 Token 限制
        var tokenLimits = _modelProviderOptions.GetTokenLimits(capability);
        var effectiveLimit = tokenLimits.EffectiveInputTokens;
        
        // 2. 估算当前上下文 Token 数
        var currentTokens = _contextManager.EstimatedTokenCount;
        
        // 3. 估算工具定义的 Token 数（如果有）
        var toolDefinitionTokens = EstimateToolDefinitionTokens();
        var totalEstimatedTokens = currentTokens + toolDefinitionTokens;
        
        // 4. 检查是否需要剪枝
        if (totalEstimatedTokens > effectiveLimit)
        {
            _logger.LogInformation(
                "Token limit exceeded. Current: {Current}, Limit: {Limit}. " +
                "Triggering context trimming with {Trimmer}.",
                totalEstimatedTokens, effectiveLimit, _contextTrimmer.Name);
            
            // 计算剪枝目标（保留一定余量）
            var targetTokens = (int)(effectiveLimit * _options.TrimTargetRatio) - toolDefinitionTokens;
            
            // 调用剪枝器
            var trimResult = await _contextTrimmer.TrimAsync(
                _contextManager,
                targetTokens,
                cancellationToken);
            
            if (!trimResult.IsSuccess)
            {
                _logger.LogWarning(
                    "Context trimming failed: {Error}",
                    trimResult.ErrorMessage);
            }
        }
        
        // 5. 获取剪枝后的消息列表
        return await _contextManager.GetMessagesForCompletionAsync(
            effectiveLimit - toolDefinitionTokens,
            cancellationToken);
    }
    
    /// <summary>
    /// Tool Loop 主循环
    /// </summary>
    protected async Task<AgentResult> ExecuteToolLoopAsync(
        string userInput,
        CancellationToken cancellationToken = default)
    {
        // 添加用户输入
        await _contextManager.AddMessageAsync(new Message
        {
            Role = MessageRole.User,
            Parts = new List<PartBase> { new TextPart { Text = userInput } }
        }, cancellationToken);
        
        var iteration = 0;
        while (!cancellationToken.IsCancellationRequested && iteration < _options.MaxIterations)
        {
            iteration++;
            
            // 准备消息（包含 Token 检查和剪枝）
            var messages = await PrepareMessagesForCompletionAsync(
                ModelCapability.Large,
                cancellationToken);
            
            // 调用 LLM
            var response = await CallLlmAsync(messages, cancellationToken);
            
            // 添加助手响应到上下文
            await _contextManager.AddMessageAsync(response.Message, cancellationToken);
            
            // 检查是否有工具调用
            var toolCalls = ExtractToolCalls(response.Message);
            if (!toolCalls.Any())
            {
                // 没有工具调用，返回最终结果
                return AgentResult.Success(response.Message);
            }
            
            // 执行工具调用
            var toolResults = await ExecuteToolCallsAsync(toolCalls, cancellationToken);
            
            // 添加工具结果到上下文
            await _contextManager.AddMessageAsync(new Message
            {
                Role = MessageRole.Tool,
                Parts = toolResults.Select(r => (PartBase)r).ToList()
            }, cancellationToken);
        }
        
        if (iteration >= _options.MaxIterations)
        {
            return AgentResult.Failure($"Exceeded maximum iterations ({_options.MaxIterations})");
        }
        
        return AgentResult.Cancelled();
    }
    
    // ... 其他抽象方法
    protected abstract Task<LlmResponse> CallLlmAsync(
        IReadOnlyList<Message> messages,
        CancellationToken cancellationToken);
    
    protected abstract IEnumerable<ToolCallPart> ExtractToolCalls(Message message);
    
    protected abstract Task<IEnumerable<ToolResultPart>> ExecuteToolCallsAsync(
        IEnumerable<ToolCallPart> toolCalls,
        CancellationToken cancellationToken);
    
    protected abstract int EstimateToolDefinitionTokens();
}
```

### 6.4 业务层自定义剪枝器示例

业务层可以实现自己的 `IContextTrimmer`，例如带 LLM 总结功能的剪枝器：

```csharp
/// <summary>
/// 带 LLM 总结功能的剪枝器
/// 业务层自定义实现
/// </summary>
public class SummarizingContextTrimmer : IContextTrimmer
{
    private readonly IContextTrimmer _fallbackTrimmer;
    private readonly IChatClient _chatClient;
    private readonly ITokenEstimator _tokenEstimator;
    private readonly ILogger<SummarizingContextTrimmer> _logger;
    
    public string Name => "Summarizing";
    
    public SummarizingContextTrimmer(
        IChatClient chatClient,
        ITokenEstimator tokenEstimator,
        IContextTrimmer? fallbackTrimmer = null,
        ILogger<SummarizingContextTrimmer>? logger = null)
    {
        _chatClient = chatClient;
        _tokenEstimator = tokenEstimator;
        _fallbackTrimmer = fallbackTrimmer ?? new RemoveOldestContextTrimmer();
        _logger = logger ?? NullLogger<SummarizingContextTrimmer>.Instance;
    }
    
    public async Task<TrimResult> TrimAsync(
        IContextManager contextManager,
        int targetTokens,
        CancellationToken cancellationToken = default)
    {
        var allMessages = await contextManager.GetAllMessagesAsync(cancellationToken);
        var tokensBefore = allMessages.Sum(m => m.Metadata.EstimatedTokens);
        
        if (tokensBefore <= targetTokens)
        {
            return TrimResult.Success(tokensBefore, tokensBefore, 0, Name);
        }
        
        try
        {
            // 计算需要总结的消息和需要保留的消息
            var (messagesToSummarize, messagesToKeep) = SplitMessages(allMessages, targetTokens);
            
            if (!messagesToSummarize.Any())
            {
                // 没有可总结的消息，退化到默认策略
                return await _fallbackTrimmer.TrimAsync(contextManager, targetTokens, cancellationToken);
            }
            
            // 生成总结
            var summary = await GenerateSummaryAsync(messagesToSummarize, cancellationToken);
            
            // 创建总结消息
            var summaryMessage = new Message
            {
                Role = MessageRole.System,
                Parts = new List<PartBase>
                {
                    new TextPart { Text = $"[历史对话总结]\n{summary}" }
                },
                Metadata = new MessageMetadata
                {
                    Priority = MessagePriority.Important,
                    IsDeletable = false,
                    Tags = new HashSet<string> { "summary" },
                    EstimatedTokens = _tokenEstimator.EstimateTokens(summary)
                }
            };
            
            // 重建上下文
            await contextManager.ClearAsync(cancellationToken);
            
            // 保留 System 消息
            var systemMsg = allMessages.FirstOrDefault(m => m.Role == MessageRole.System);
            if (systemMsg != null)
            {
                await contextManager.AddMessageAsync(systemMsg, cancellationToken);
            }
            
            // 添加总结消息
            await contextManager.AddMessageAsync(summaryMessage, cancellationToken);
            
            // 添加保留的消息
            await contextManager.AddMessagesAsync(messagesToKeep, cancellationToken);
            
            var tokensAfter = contextManager.EstimatedTokenCount;
            
            return TrimResult.Success(tokensBefore, tokensAfter, messagesToSummarize.Count, Name);
        }
        catch (Exception ex)
        {
            _logger.LogWarning(ex, "Summarization failed, falling back to {Fallback}", _fallbackTrimmer.Name);
            return await _fallbackTrimmer.TrimAsync(contextManager, targetTokens, cancellationToken);
        }
    }
    
    private (List<Message> ToSummarize, List<Message> ToKeep) SplitMessages(
        IReadOnlyList<Message> messages,
        int targetTokens)
    {
        var toKeep = new List<Message>();
        var currentTokens = 0;
        
        // 预留 20% 给总结
        var reservedForSummary = (int)(targetTokens * 0.2);
        var availableForMessages = targetTokens - reservedForSummary;
        
        // 从后向前，保留最新的消息
        var nonSystemMessages = messages
            .Where(m => m.Role != MessageRole.System)
            .Reverse()
            .ToList();
        
        foreach (var msg in nonSystemMessages)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens <= availableForMessages)
            {
                toKeep.Insert(0, msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
        }
        
        // 剩余的消息需要总结
        var toSummarize = messages
            .Where(m => m.Role != MessageRole.System && !toKeep.Contains(m))
            .ToList();
        
        return (toSummarize, toKeep);
    }
    
    private async Task<string> GenerateSummaryAsync(
        List<Message> messages,
        CancellationToken cancellationToken)
    {
        var conversationText = FormatMessagesForSummary(messages);
        
        var prompt = $"""
            请总结以下对话的关键信息，保留：
            1. 重要的发现和结论
            2. 做出的关键决策
            3. 待处理的问题
            4. 相关的工具调用结果摘要
            
            对话内容：
            {conversationText}
            
            请用简洁的中文输出总结，控制在 500 字以内。
            """;
        
        var response = await _chatClient.CompleteAsync(prompt, cancellationToken: cancellationToken);
        return response.Message.Text ?? "";
    }
    
    private string FormatMessagesForSummary(List<Message> messages)
    {
        var sb = new StringBuilder();
        foreach (var msg in messages)
        {
            var role = msg.Role switch
            {
                MessageRole.User => "用户",
                MessageRole.Assistant => "助手",
                MessageRole.Tool => "工具结果",
                _ => msg.Role.ToString()
            };
            
            foreach (var part in msg.Parts)
            {
                var content = part switch
                {
                    TextPart text => text.Text,
                    ToolCallPart toolCall => $"[调用工具: {toolCall.Name}]",
                    ToolResultPart toolResult => $"[{toolResult.ToolName} 结果: {Truncate(toolResult.Content, 200)}]",
                    _ => ""
                };
                
                if (!string.IsNullOrEmpty(content))
                {
                    sb.AppendLine($"{role}: {content}");
                }
            }
        }
        return sb.ToString();
    }
    
    private static string Truncate(string text, int maxLength)
    {
        if (text.Length <= maxLength) return text;
        return text[..maxLength] + "...";
    }
}
```

### 6.5 使用示例

```csharp
// 示例 1：使用默认剪枝器（RemoveOldest）
var agent = new MySreAgent(
    contextManager,
    tokenEstimator,
    modelProviderOptions);

// 示例 2：使用优先级剪枝器
var agent = new MySreAgent(
    contextManager,
    tokenEstimator,
    modelProviderOptions,
    new ToolLoopAgentOptions
    {
        ContextTrimmer = new PriorityBasedContextTrimmer()
    });

// 示例 3：使用滑动窗口剪枝器
var agent = new MySreAgent(
    contextManager,
    tokenEstimator,
    modelProviderOptions,
    new ToolLoopAgentOptions
    {
        ContextTrimmer = new SlidingWindowContextTrimmer()
    });

// 示例 4：使用带 LLM 总结的剪枝器（业务层实现）
var agent = new MySreAgent(
    contextManager,
    tokenEstimator,
    modelProviderOptions,
    new ToolLoopAgentOptions
    {
        ContextTrimmer = new SummarizingContextTrimmer(
            chatClient,
            tokenEstimator,
            fallbackTrimmer: new RemoveOldestContextTrimmer())
    });
```

### 6.6 依赖注入配置

```csharp
// 注册 Framework 提供的剪枝器
services.AddSingleton<RemoveOldestContextTrimmer>();
services.AddSingleton<PriorityBasedContextTrimmer>();
services.AddSingleton<SlidingWindowContextTrimmer>();

// 默认使用 RemoveOldest
services.AddSingleton<IContextTrimmer, RemoveOldestContextTrimmer>();

// 或者根据配置选择
services.AddSingleton<IContextTrimmer>(sp =>
{
    var config = sp.GetRequiredService<IConfiguration>();
    var strategy = config.GetValue<string>("ContextTrimmer:Strategy") ?? "RemoveOldest";
    
    return strategy switch
    {
        "PriorityBased" => sp.GetRequiredService<PriorityBasedContextTrimmer>(),
        "SlidingWindow" => sp.GetRequiredService<SlidingWindowContextTrimmer>(),
        _ => sp.GetRequiredService<RemoveOldestContextTrimmer>()
    };
});
```

---

## 7. 默认实现

### 7.1 DefaultContextManager

```csharp
/// <summary>
/// 默认上下文管理器实现
/// </summary>
public class DefaultContextManager : IContextManager
{
    private readonly List<Message> _messages = new();
    private readonly ITokenEstimator _tokenEstimator;
    private readonly ContextManagerOptions _options;
    private Message? _systemMessage;
    private readonly object _lock = new();
    
    public DefaultContextManager(
        ITokenEstimator tokenEstimator,
        ContextManagerOptions? options = null)
    {
        _tokenEstimator = tokenEstimator;
        _options = options ?? new ContextManagerOptions();
    }
    
    public int EstimatedTokenCount
    {
        get
        {
            lock (_lock)
            {
                var total = _systemMessage != null 
                    ? _systemMessage.Metadata.EstimatedTokens 
                    : 0;
                return total + _messages.Sum(m => m.Metadata.EstimatedTokens);
            }
        }
    }
    
    public Task SetSystemMessageAsync(string content, CancellationToken cancellationToken = default)
    {
        lock (_lock)
        {
            _systemMessage = new Message
            {
                Role = MessageRole.System,
                Parts = new List<PartBase> { new TextPart { Text = content } },
                Metadata = new MessageMetadata
                {
                    IsDeletable = false,
                    Priority = MessagePriority.Critical,
                    EstimatedTokens = _tokenEstimator.EstimateTokens(content)
                }
            };
        }
        return Task.CompletedTask;
    }
    
    public Task AddMessageAsync(Message message, CancellationToken cancellationToken = default)
    {
        // 计算 Token 数
        message.Metadata.EstimatedTokens = _tokenEstimator.EstimateTokens(message);
        
        // 自动压缩长工具结果
        if (_options.AutoCompressToolResults)
        {
            CompressToolResultsIfNeeded(message);
        }
        
        lock (_lock)
        {
            _messages.Add(message);
        }
        
        return Task.CompletedTask;
    }
    
    public Task AddMessagesAsync(IEnumerable<Message> messages, CancellationToken cancellationToken = default)
    {
        foreach (var message in messages)
        {
            AddMessageAsync(message, cancellationToken);
        }
        return Task.CompletedTask;
    }
    
    public Task<IReadOnlyList<Message>> GetMessagesForCompletionAsync(
        int maxTokens,
        CancellationToken cancellationToken = default)
    {
        lock (_lock)
        {
            var allMessages = BuildMessageList();
            var currentTokens = allMessages.Sum(m => m.Metadata.EstimatedTokens);
            
            // 如果未超限，直接返回
            if (currentTokens <= maxTokens)
            {
                return Task.FromResult<IReadOnlyList<Message>>(allMessages);
            }
            
            // 需要裁剪
            var trimmed = TrimMessages(allMessages, maxTokens);
            return Task.FromResult<IReadOnlyList<Message>>(trimmed);
        }
    }
    
    public Task<IReadOnlyList<Message>> GetAllMessagesAsync(
        CancellationToken cancellationToken = default)
    {
        lock (_lock)
        {
            return Task.FromResult<IReadOnlyList<Message>>(BuildMessageList());
        }
    }
    
    public Task ClearAsync(CancellationToken cancellationToken = default)
    {
        lock (_lock)
        {
            _messages.Clear();
            // 注意：不清除 System 消息
        }
        return Task.CompletedTask;
    }
    
    private List<Message> BuildMessageList()
    {
        var result = new List<Message>();
        if (_systemMessage != null)
        {
            result.Add(_systemMessage);
        }
        result.AddRange(_messages);
        return result;
    }
    
    private List<Message> TrimMessages(List<Message> messages, int maxTokens)
    {
        return _options.TrimStrategy switch
        {
            TrimStrategy.RemoveOldest => TrimByRemovingOldest(messages, maxTokens),
            TrimStrategy.RemoveByPriority => TrimByPriority(messages, maxTokens),
            TrimStrategy.SlidingWindow => TrimBySlidingWindow(messages, maxTokens),
            _ => TrimByRemovingOldest(messages, maxTokens)
        };
    }
    
    private List<Message> TrimByRemovingOldest(List<Message> messages, int maxTokens)
    {
        var result = new List<Message>();
        var currentTokens = 0;
        
        // 始终保留 System 消息
        var systemMsg = messages.FirstOrDefault(m => m.Role == MessageRole.System);
        if (systemMsg != null)
        {
            result.Add(systemMsg);
            currentTokens += systemMsg.Metadata.EstimatedTokens;
        }
        
        // 从后向前遍历，保留最新的消息
        var nonSystemMessages = messages
            .Where(m => m.Role != MessageRole.System)
            .Reverse()
            .ToList();
        
        foreach (var msg in nonSystemMessages)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens > maxTokens)
            {
                // 如果是不可删除的消息，强制保留
                if (!msg.Metadata.IsDeletable)
                {
                    result.Insert(result.Count > 0 && result[0].Role == MessageRole.System ? 1 : 0, msg);
                    currentTokens += msg.Metadata.EstimatedTokens;
                }
                continue;
            }
            
            result.Insert(result.Count > 0 && result[0].Role == MessageRole.System ? 1 : 0, msg);
            currentTokens += msg.Metadata.EstimatedTokens;
        }
        
        return result;
    }
    
    private List<Message> TrimByPriority(List<Message> messages, int maxTokens)
    {
        // 按优先级排序，优先保留高优先级消息
        var sorted = messages
            .OrderByDescending(m => m.Metadata.Priority)
            .ThenByDescending(m => m.CreatedAt)
            .ToList();
        
        var result = new List<Message>();
        var currentTokens = 0;
        
        foreach (var msg in sorted)
        {
            if (currentTokens + msg.Metadata.EstimatedTokens <= maxTokens)
            {
                result.Add(msg);
                currentTokens += msg.Metadata.EstimatedTokens;
            }
        }
        
        // 按时间重新排序
        return result.OrderBy(m => m.CreatedAt).ToList();
    }
    
    private List<Message> TrimBySlidingWindow(List<Message> messages, int maxTokens)
    {
        var result = new List<Message>();
        
        // 保留 System 消息
        var systemMsg = messages.FirstOrDefault(m => m.Role == MessageRole.System);
        if (systemMsg != null)
        {
            result.Add(systemMsg);
        }
        
        // 保留最近 N 条消息
        var nonSystemMessages = messages
            .Where(m => m.Role != MessageRole.System)
            .ToList();
        
        var remainingTokens = maxTokens - (systemMsg?.Metadata.EstimatedTokens ?? 0);
        var windowMessages = new List<Message>();
        
        for (int i = nonSystemMessages.Count - 1; i >= 0; i--)
        {
            var msg = nonSystemMessages[i];
            if (windowMessages.Sum(m => m.Metadata.EstimatedTokens) + msg.Metadata.EstimatedTokens <= remainingTokens)
            {
                windowMessages.Insert(0, msg);
            }
            else
            {
                break;
            }
        }
        
        result.AddRange(windowMessages);
        return result;
    }
    
    private void CompressToolResultsIfNeeded(Message message)
    {
        foreach (var part in message.Parts.OfType<ToolResultPart>().ToList())
        {
            var tokens = _tokenEstimator.EstimateTokens(part.Content);
            if (tokens > _options.ToolResultCompressThreshold)
            {
                // 压缩长结果
                var compressed = CompressContent(part.Content, _options.ToolResultCompressThreshold);
                var index = message.Parts.IndexOf(part);
                message.Parts[index] = part with { Content = compressed };
            }
        }
    }
    
    private string CompressContent(string content, int targetTokens)
    {
        // 简单的截断策略，实际可以更智能
        var lines = content.Split('\n');
        var result = new List<string>();
        var currentTokens = 0;
        
        foreach (var line in lines)
        {
            var lineTokens = _tokenEstimator.EstimateTokens(line);
            if (currentTokens + lineTokens > targetTokens)
            {
                result.Add("... [content truncated] ...");
                break;
            }
            result.Add(line);
            currentTokens += lineTokens;
        }
        
        return string.Join('\n', result);
    }
}
```

---

## 8. Token 估算器

### 8.1 接口定义

```csharp
/// <summary>
/// Token 估算器接口
/// </summary>
public interface ITokenEstimator
{
    int EstimateTokens(string text);
    int EstimateTokens(Message message);
    int EstimateTokens(IEnumerable<Message> messages);
}
```

### 8.2 实现

```csharp
/// <summary>
/// 基于 Tiktoken 的 Token 估算器
/// </summary>
public class TiktokenEstimator : ITokenEstimator
{
    private readonly TikToken _encoder;
    
    public TiktokenEstimator(string model = "gpt-4")
    {
        _encoder = TikToken.EncodingForModel(model);
    }
    
    public int EstimateTokens(string text)
    {
        if (string.IsNullOrEmpty(text)) return 0;
        return _encoder.Encode(text).Count;
    }
    
    public int EstimateTokens(Message message)
    {
        // 消息格式化开销约 4 tokens
        const int messageOverhead = 4;
        
        var contentTokens = 0;
        foreach (var part in message.Parts)
        {
            contentTokens += part switch
            {
                TextPart text => EstimateTokens(text.Text),
                ToolCallPart toolCall => EstimateTokens(toolCall.Name) + EstimateTokens(toolCall.Arguments),
                ToolResultPart toolResult => EstimateTokens(toolResult.Content),
                _ => 0
            };
        }
        
        return messageOverhead + contentTokens;
    }
    
    public int EstimateTokens(IEnumerable<Message> messages)
    {
        return messages.Sum(EstimateTokens);
    }
}

/// <summary>
/// 简单的字符估算器（用于测试或无法使用 Tiktoken 时）
/// </summary>
public class SimpleTokenEstimator : ITokenEstimator
{
    private const double CharsPerToken = 4.0;
    
    public int EstimateTokens(string text)
    {
        if (string.IsNullOrEmpty(text)) return 0;
        return (int)Math.Ceiling(text.Length / CharsPerToken);
    }
    
    public int EstimateTokens(Message message)
    {
        return message.Parts.Sum(part => part switch
        {
            TextPart text => EstimateTokens(text.Text),
            ToolCallPart toolCall => EstimateTokens(toolCall.Name) + EstimateTokens(toolCall.Arguments),
            ToolResultPart toolResult => EstimateTokens(toolResult.Content),
            _ => 0
        }) + 4;
    }
    
    public int EstimateTokens(IEnumerable<Message> messages)
    {
        return messages.Sum(EstimateTokens);
    }
}
```

---

## 9. 业务层扩展示例

### 9.1 带总结的上下文管理器

```csharp
/// <summary>
/// 支持历史消息总结的上下文管理器
/// </summary>
public class SummarizingContextManager : IContextManager
{
    private readonly IContextManager _inner;
    private readonly IChatClient _chatClient;
    private readonly ITokenEstimator _tokenEstimator;
    private readonly SummarizingOptions _options;
    
    private Message? _summaryMessage;
    
    public async Task<IReadOnlyList<Message>> GetMessagesForCompletionAsync(
        int maxTokens,
        CancellationToken cancellationToken = default)
    {
        var allMessages = await _inner.GetAllMessagesAsync(cancellationToken);
        var currentTokens = allMessages.Sum(m => m.Metadata.EstimatedTokens);
        
        if (currentTokens <= maxTokens)
        {
            return allMessages;
        }
        
        // 需要总结历史消息
        var (messagesToSummarize, recentMessages) = SplitMessages(allMessages, maxTokens);
        
        if (messagesToSummarize.Any())
        {
            _summaryMessage = await GenerateSummaryAsync(messagesToSummarize, cancellationToken);
        }
        
        var result = new List<Message>();
        
        // System 消息
        var systemMsg = allMessages.FirstOrDefault(m => m.Role == MessageRole.System);
        if (systemMsg != null)
        {
            result.Add(systemMsg);
        }
        
        // 总结消息
        if (_summaryMessage != null)
        {
            result.Add(_summaryMessage);
        }
        
        // 近期消息
        result.AddRange(recentMessages);
        
        return result;
    }
    
    private async Task<Message> GenerateSummaryAsync(
        IEnumerable<Message> messages,
        CancellationToken cancellationToken)
    {
        var conversationText = FormatMessagesForSummary(messages);
        
        var prompt = $"""
            请总结以下对话的关键信息，保留：
            1. 重要的发现和结论
            2. 做出的关键决策
            3. 待处理的问题
            4. 相关的工具调用结果摘要
            
            对话内容：
            {conversationText}
            
            请用简洁的中文输出总结。
            """;
        
        var response = await _chatClient.CompleteAsync(prompt, cancellationToken: cancellationToken);
        
        return new Message
        {
            Role = MessageRole.System,
            Parts = new List<PartBase>
            {
                new TextPart { Text = $"[历史对话总结]\n{response.Message.Text}" }
            },
            Metadata = new MessageMetadata
            {
                Priority = MessagePriority.Important,
                IsDeletable = false,
                Tags = new HashSet<string> { "summary" },
                EstimatedTokens = _tokenEstimator.EstimateTokens(response.Message.Text ?? "")
            }
        };
    }
    
    // ... 其他方法委托给 _inner
}
```

### 9.2 带持久化的上下文管理器

```csharp
/// <summary>
/// 支持持久化的上下文管理器
/// </summary>
public class PersistentContextManager : IContextManager
{
    private readonly IContextManager _inner;
    private readonly IMessageRepository _repository;
    private readonly Guid _sessionId;
    
    public async Task AddMessageAsync(Message message, CancellationToken cancellationToken = default)
    {
        // 添加到内存
        await _inner.AddMessageAsync(message, cancellationToken);
        
        // 持久化
        await _repository.SaveAsync(_sessionId, message, cancellationToken);
    }
    
    public async Task<IReadOnlyList<Message>> GetAllMessagesAsync(
        CancellationToken cancellationToken = default)
    {
        // 优先从内存获取
        var inMemory = await _inner.GetAllMessagesAsync(cancellationToken);
        if (inMemory.Any())
        {
            return inMemory;
        }
        
        // 从数据库加载
        var fromDb = await _repository.GetBySessionAsync(_sessionId, cancellationToken);
        await _inner.AddMessagesAsync(fromDb, cancellationToken);
        
        return fromDb;
    }
    
    // ... 其他方法
}
```

---

## 10. Skill 渐进式加载

### 10.1 Skill 定义

```csharp
/// <summary>
/// Skill - 可按需加载的知识片段
/// </summary>
public class Skill
{
    public string Id { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
    
    /// <summary>简短描述（始终在上下文中）</summary>
    public string ShortDescription { get; set; } = string.Empty;
    
    /// <summary>详细说明（按需加载）</summary>
    public string DetailedInstructions { get; set; } = string.Empty;
    
    public List<string> Tags { get; set; } = new();
    public int EstimatedTokens { get; set; }
}
```

### 10.2 Skill 加载工具

```csharp
/// <summary>
/// 加载 Skill 的工具
/// </summary>
public class LoadSkillTool : ITool
{
    private readonly ISkillRepository _skillRepository;
    
    public string Name => "load_skill";
    public string Description => "Load detailed instructions for a specific skill when needed";
    
    public JsonElement ParameterSchema => JsonDocument.Parse("""
        {
            "type": "object",
            "properties": {
                "skill_id": {
                    "type": "string",
                    "description": "The ID of the skill to load"
                }
            },
            "required": ["skill_id"]
        }
        """).RootElement;
    
    public async Task<ToolResult> ExecuteAsync(
        ToolExecutionContext context,
        CancellationToken cancellationToken = default)
    {
        try
        {
            var skillId = context.Parameters.GetProperty("skill_id").GetString();
            if (string.IsNullOrEmpty(skillId))
            {
                return ToolResult.Failure("skill_id is required", "INVALID_PARAM");
            }
            
            var skill = await _skillRepository.GetAsync(skillId, cancellationToken);
            if (skill == null)
            {
                return ToolResult.Failure($"Skill '{skillId}' not found", "NOT_FOUND");
            }
            
            return ToolResult.Success(skill.DetailedInstructions, skill);
        }
        catch (Exception ex)
        {
            return ToolResult.FromException(ex);
        }
    }
}
```

### 10.3 在 System Prompt 中引用

```
你是一个 SRE 诊断专家。

可用的 Skills（使用 load_skill 工具加载详细说明）：
- sumo_logic_query: Sumo Logic 日志查询技巧
- promql_basics: Prometheus PromQL 基础语法
- k8s_troubleshooting: Kubernetes 故障排查方法
- aws_rds_diagnosis: AWS RDS 问题诊断

当你需要某个领域的详细指导时，请使用 load_skill 工具加载对应的说明。
```

---

## 11. 配置示例

```yaml
context_manager:
  # 默认配置
  default_max_tokens: 100000
  trim_threshold: 0.8
  trim_target: 0.6
  trim_strategy: "remove_oldest"
  
  # 工具结果压缩
  auto_compress_tool_results: true
  tool_result_compress_threshold: 1000
  
  # Token 估算器
  token_estimator: "tiktoken"  # 或 "simple"
  tiktoken_model: "gpt-4"

# 业务层可覆盖
sre_agent:
  context_manager:
    trim_strategy: "summarize"
    enable_persistence: true
```
