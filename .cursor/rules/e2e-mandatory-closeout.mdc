---
description: E2E verification required after each completed task
alwaysApply: true
---

# 每任务完成后必须执行 E2E

每个任务从 `in_progress` 变为 `done` 前，必须完成端到端验证。

## 测试策略原则

1. **前端优先原则**：当一个功能同时涉及后端 API 和前端页面时，优先通过前端浏览器触发 E2E 测试，不再单独测试 API，避免重复测试造成的资源浪费。
2. **浏览器必开原则**：凡涉及前端改动的任务，E2E 阶段**必须**打开浏览器执行验证，不得仅用 curl 等方式替代。
3. **CORS 真实端口验证**：浏览器 E2E 必须使用用户实际使用的前端端口（如 `localhost:5173`），不得仅在白名单端口上验证。

## 分场景要求

### 纯后端任务（无前端页面对应）

- 使用 `e2e-test` 子代理或 curl 验证 API 链路
- 验证分析结果与链路日志

### 涉及前端的任务（前端+后端联动）

- **只需从前端触发测试**，通过浏览器走完关键用户路径即可覆盖 API 验证
- 无需单独再用 curl 测试已被前端覆盖的 API
- 覆盖核心功能、空状态、错误分支
- 记录关键截图或结论

### 纯前端任务

- 打开浏览器执行关键用户路径
- 验证 UI 状态（加载/成功/空/错误）
- 验证与设计稿的一致性

## 执行方式

- 涉及前端的任务：使用 `browser-use` 子代理执行浏览器验证
- 纯后端链路任务：使用 `e2e-test` 子代理执行验证

未完成 E2E 的任务不得标记为 `done`，并需在看板中标记为 `blocked` 或 `in_progress`。
