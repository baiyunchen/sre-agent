---
description: Large change task slicing and progress tracking
alwaysApply: true
---

# 大型改动任务拆分与进度维护

当需求属于“较大改动”（跨模块、涉及 API、或预计修改多个文件）时，必须先建立并维护任务看板：

- 统一使用 `docs/13-大型变更任务看板.md` 记录任务
- 在开始编码前先写任务，再进入实现
- 每个任务尽量可在半天内闭环，避免过大任务
- 任务状态仅使用：`todo` / `in_progress` / `blocked` / `done`
- 同一时间只允许一个 `in_progress` 主任务，减少并行上下文污染

每次阶段性完成后都要更新看板：

1. 更新任务状态与时间戳
2. 记录已完成代码范围
3. 记录测试执行结果与覆盖率
4. 记录阻塞项与下一步

如果任务被拆分或范围变化，必须立即同步到看板，确保进度可追踪、可审计、可恢复。
